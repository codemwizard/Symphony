schema_version: 1
phase: '0.2'
task_id: R-001
title: Fail hard on missing signing keys
owner_role: SUPERVISOR
status: completed
depends_on:
- R-000
touches:
- services/ledger-api/dotnet/src/LedgerApi/Program.cs
invariants:
- '{''id'': ''R001-INV-1'', ''description'': ''Startup `/health` correctly flags `signing_key_present:
  false` when key is absent.'', ''enforcement_type'': ''dynamic''}'
- '{''id'': ''R001-INV-2'', ''description'': ''Regulatory endpoints return 503 HTTP
  status exactly when signing capability is missing.'', ''enforcement_type'': ''dynamic''}'
- '{''id'': ''R001-INV-3'', ''description'': ''No literal `dev-signing-key` usage
  in Codebase.'', ''enforcement_type'': ''static''}'
work: []
acceptance_criteria:
- id: R001-AC-1
  description: Grepping for `dev-signing-key` or `phase1-reg-.*-dev-signing-key` yields
    zero hits.
- id: R001-AC-2
  description: Semgrep scan ensures `?? "literal"` fallbacks for EVIDENCE_SIGNING_KEY
    are eradicated structurally.
- id: R001-AC-3
  description: Unconfigured application starts up gracefully but endpoints fail-closed
    deterministically with 503.
verification:
- SYMPHONY_ENV=development bash scripts/audit/test_missing_signing_key_fails_closed.sh
evidence:
- evidence/security_remediation/r_001_signing_keys.json
failure_modes: []
must_read: []
implementation_plan: docs/plans/remediation/r_001/PLAN.md
implementation_log: ''
notes: Remove fallback logic `?? "dev-signing-key"` and enforce strict signature capabilities.
  Ensures reporting does not emit unsigned data gracefully.
client: codex_cli
assigned_agent: supervisor
model: <UNASSIGNED>
