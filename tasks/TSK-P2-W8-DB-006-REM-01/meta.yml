schema_version: 1
phase: '2'
task_id: TSK-P2-W8-DB-006-REM-01
title: "Fix policy_decisions append-only trigger regression"
owner_role: DB_FOUNDATION_AGENT
status: completed
priority: HIGH
risk_class: SCHEMA_INTEGRITY
blast_radius: SCHEMA_MIGRATIONS

intent: >-
  Fix migration trigger to block both UPDATE and DELETE operations on policy_decisions table
  to restore append-only immutability guarantees that were inadvertently relaxed.

primary_enforcement_domain: >-
  Policy decisions table trigger logic and append-only enforcement

anti_patterns:
  - "Changing trigger without verifying append-only contract requirements"
  - "Relaxing schema constraints without proper testing"

out_of_scope:
  - "Changes to other policy_decisions table migrations"
  - "Non-append-only related schema changes"

stop_conditions:
  - "If fix requires changes to multiple tables, stop and split into separate tasks"
  - "If baseline drift is detected during fix, stop and address drift first"

proof_guarantees:
  - "Trigger blocks both UPDATE and DELETE operations"
  - "Migration does not introduce baseline drift"
  - "Append-only contract is fully restored"

proof_limitations:
  - "This fix only addresses policy_decisions table trigger"

depends_on:
  - TSK-P2-W8-DB-006

blocks: []

touches:
  - schema/migrations/
  - scripts/db/
  - evidence/phase2/

invariants:
  - INV-SCHEMA-001
  - INV-APPEND-ONLY-001

deliverable_files:
  - Fixed migration file
  - evidence/phase2/policy_decisions_append_only_fix.json

regulated_surface_compliance:
  enabled: true
  approval_workflow: stage_a_only
  stage_a_required_before_edit: true
  regulated_paths:
    - evidence/phase2/policy_decisions_append_only_fix.json
    - schema/migrations/
  must_read:
    - docs/operations/REGULATED_SURFACE_PATHS.yml

remediation_trace_compliance:
  enabled: true
  required_markers:
    - failure_signature
    - origin_task_id
    - repro_command
    - verification_commands_run
    - final_status
  marker_location: EXEC_LOG.md
  append_only: true
  markers_required_at_edit: true
  must_read:
    - docs/operations/REMEDIATION_TRACE_WORKFLOW.md

database_connection:
  enabled: false

migration_dependencies:
  enabled: true

work:
  - >-
    [ID w8_db_006_rem_01_work_01] Restore trigger condition to block both UPDATE and DELETE
    operations on policy_decisions table to maintain append-only immutability.

  - >-
    [ID w8_db_006_rem_01_work_02] Verify fix does not introduce baseline drift and
    append-only enforcement works correctly.

acceptance_criteria:
  - >-
    [ID w8_db_006_rem_01_work_01] Trigger blocks both UPDATE and DELETE operations
    with GF060 exception.

  - >-
    [ID w8_db_006_rem_01_work_02] Migration passes baseline drift check and
    append-only verification tests.

negative_tests:
  - id: TSK-P2-W8-DB-006-REM-01-N1
    description: >-
      Verifier fails if trigger still allows UPDATE operations after fix.

positive_tests:
  - id: TSK-P2-W8-DB-006-REM-01-P1
    description: >-
      Verifier passes when trigger blocks both UPDATE and DELETE operations
      and baseline drift check passes.

verification:
  - >-
    [ID w8_db_006_rem_01_work_01] [ID w8_db_006_rem_01_work_02] test -e
    schema/migrations/0203_converge_policy_decisions_schema.sql && DATABASE_URL="postgresql://symphony_admin:symphony_pass@localhost:55432/symphony" scripts/db/verify_invariants.sh

evidence:
  - path: evidence/phase2/policy_decisions_append_only_fix.json
    must_include:
      - task_id
      - git_sha
      - timestamp_utc
      - status
      - checks
      - observed_paths
      - observed_hashes
      - command_outputs
      - execution_trace

failure_modes:
  - "Migration still allows UPDATE operations => FAIL"
  - "Baseline drift introduced => CRITICAL_FAIL"
  - "Missing evidence file => FAIL"

must_read:
  - docs/operations/AI_AGENT_OPERATION_MANUAL.md
  - AGENT_ENTRYPOINT.md
  - docs/operations/REMEDIATION_TRACE_WORKFLOW.md
  - docs/operations/AGENT_PROMPT_ROUTER.md
  - docs/operations/TASK_CREATION_PROCESS.md
  - docs/operations/WAVE5_TASK_CREATION_LESSONS_LEARNED.md
  - docs/PLANS-addendum_1.md
  - tasks/TSK-P1-240/meta.yml

implementation_plan: docs/plans/phase2/REM-2026-05-05_policy_decisions_append_only_regression/PLAN.md
implementation_log: docs/plans/phase2/REM-2026-05-05_policy_decisions_append_only_regression/EXEC_LOG.md

notes: >-
  Derived from DRD REM-2026-05-05_policy_decisions_append_only_regression
  to fix critical schema integrity regression in append-only enforcement.

client: codex
assigned_agent: db_foundation
model: gpt-5.5
