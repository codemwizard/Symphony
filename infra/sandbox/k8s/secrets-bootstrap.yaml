apiVersion: v1
kind: Secret
metadata:
  name: symphony-pilot-secrets
  namespace: symphony-pilot
type: Opaque
stringData:
  DATABASE_URL: ${DATABASE_URL}
  SYMPHONY_PILOT_API_KEYS: ${SYMPHONY_PILOT_API_KEYS}
