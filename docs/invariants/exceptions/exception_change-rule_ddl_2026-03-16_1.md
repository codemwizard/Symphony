---
exception_id: EXC-217
inv_scope: change-rule
expiry: 2026-04-16
follow_up_ticket: TSK-P1-217
reason: Wave 2 hardening adds onboarding control-plane tables (tenant_registry, programme_registry, programme_policy_binding) per TSK-P1-217.
author: supervisor
created_at: 2026-03-16
---

# Exception: ddl structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

TSK-P1-217 introduces persisted onboarding control-plane state. The DDL creates tables for tenant governance, programme lifecycle, and policy binding with FK constraints, RLS policies, and audit timestamps. This replaces environment-driven SYMPHONY_KNOWN_TENANTS with governed persisted state.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: ddl
reason_types: ddl, migration_file_added_or_deleted

Matched files:
- schema/migrations/0076_onboarding_control_plane.sql

Top matches:
- ddl | schema/migrations/0076_onboarding_control_plane.sql | +: CREATE TABLE IF NOT EXISTS public.tenant_registry (
- ddl | schema/migrations/0076_onboarding_control_plane.sql | +: CHECK (status IN ('ACTIVE', 'SUSPENDED', 'CLOSED')),
- ddl | schema/migrations/0076_onboarding_control_plane.sql | +: ALTER TABLE public.tenant_registry ENABLE ROW LEVEL SECURITY;
- ddl | schema/migrations/0076_onboarding_control_plane.sql | +: ALTER TABLE public.tenant_registry FORCE ROW LEVEL SECURITY;
- ddl | schema/migrations/0076_onboarding_control_plane.sql | +: CREATE TABLE IF NOT EXISTS public.programme_registry (
- ddl | schema/migrations/0076_onboarding_control_plane.sql | +: tenant_id       uuid NOT NULL REFERENCES public.tenant_registry(tenant_id),
- ddl | schema/migrations/0076_onboarding_control_plane.sql | +: CHECK (status IN ('CREATED', 'ACTIVE', 'SUSPENDED', 'CLOSED')),
- ddl | schema/migrations/0076_onboarding_control_plane.sql | +: ALTER TABLE public.programme_registry ENABLE ROW LEVEL SECURITY;
- ddl | schema/migrations/0076_onboarding_control_plane.sql | +: ALTER TABLE public.programme_registry FORCE ROW LEVEL SECURITY;
- ddl | schema/migrations/0076_onboarding_control_plane.sql | +: CREATE TABLE IF NOT EXISTS public.programme_policy_binding (
- ddl | schema/migrations/0076_onboarding_control_plane.sql | +: programme_id    uuid NOT NULL REFERENCES public.programme_registry(id),
- ddl | schema/migrations/0076_onboarding_control_plane.sql | +: tenant_id       uuid NOT NULL REFERENCES public.tenant_registry(tenant_id),
- ddl | schema/migrations/0076_onboarding_control_plane.sql | +: ALTER TABLE public.programme_policy_binding ENABLE ROW LEVEL SECURITY;
- ddl | schema/migrations/0076_onboarding_control_plane.sql | +: ALTER TABLE public.programme_policy_binding FORCE ROW LEVEL SECURITY;
- ddl | scripts/audit/verify_tsk_p1_217.sh | +: if ! grep -q "REFERENCES.*tenant_registry" "$MIGRATION" 2>/dev/null; then
- migration_file_added_or_deleted | schema/migrations/0076_onboarding_control_plane.sql | +: (migration file added or deleted)

## Mitigation

- Migration is forward-only and idempotent (CREATE TABLE IF NOT EXISTS)
- RLS policies enforced on all tables
- FK constraints govern programme-to-tenant relationship
- Verifier script (verify_tsk_p1_217.sh) validates structural correctness
- Evidence generated: evidence/phase1/tsk_p1_217_onboarding_control_plane.json
