---
exception_id: EXC-314
inv_scope: change-rule
expiry: 2026-12-31
closed_at: 2026-03-21
follow_up_ticket: TSK-HARD-050-054-011B-096
reason: Auto-generated: structural change detected; invariants linkage missing in commit.
author: system
created_at: 2026-03-05
---

# Exception: ddl structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

Wave-4 introduces signing-control and assurance governance DDL plus baseline refresh.
This exception records structural change while linked invariant promotion lands.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: ddl
reason_types: ddl, migration_file_added_or_deleted, security

Matched files:
- schema/migrations/0065_hard_wave4_signing_controls_and_assurance.sql

Top matches:
- ddl | schema/baseline.sql | +: CREATE TYPE public.key_class_enum AS ENUM (
- ddl | schema/baseline.sql | +: CREATE TYPE public.policy_bundle_state_enum AS ENUM (
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- ddl | schema/baseline.sql | +: CREATE TABLE public.historical_verification_runs (
- ddl | schema/baseline.sql | +: CREATE TABLE public.key_rotation_drills (
- ddl | schema/baseline.sql | +: CONSTRAINT key_rotation_drills_drill_outcome_check CHECK ((drill_outcome = ANY (ARRAY['PASS'::text, 'FAIL'::text]))),
- ddl | schema/baseline.sql | +: CONSTRAINT key_rotation_drills_rotation_type_check CHECK ((rotation_type = ANY (ARRAY['SCHEDULED'::text, 'EMERGENCY'::text])))
- ddl | schema/baseline.sql | +: CREATE TABLE public.policy_bundles (
- ddl | schema/baseline.sql | +: CREATE TABLE public.resign_sweeps (
- ddl | schema/baseline.sql | +: CONSTRAINT resign_sweeps_artifacts_resigned_count_check CHECK ((artifacts_resigned_count >= 0))
- ddl | schema/baseline.sql | +: CREATE TABLE public.signing_audit_log (
- ddl | schema/baseline.sql | +: CONSTRAINT signing_audit_log_outcome_check CHECK ((outcome = ANY (ARRAY['PASS'::text, 'REJECTED'::text, 'BLOCKED'::text]))),
- ddl | schema/baseline.sql | +: CONSTRAINT signing_audit_log_signing_path_check CHECK ((signing_path = ANY (ARRAY['HSM'::text, 'KMS'::text, 'SOFTWARE_BYPASS'::text])))
- ddl | schema/baseline.sql | +: CREATE TABLE public.signing_authorization_matrix (
- ddl | schema/baseline.sql | +: CONSTRAINT signing_authorization_matrix_key_backend_check CHECK ((key_backend = ANY (ARRAY['HSM'::text, 'KMS'::text, 'SOFTWARE'::text])))
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.historical_verification_runs
- ddl | schema/baseline.sql | +: ADD CONSTRAINT historical_verification_runs_pkey PRIMARY KEY (verification_run_id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.key_rotation_drills

## Mitigation

Mitigations:
- Full `RUN_PHASE1_GATES=1 scripts/dev/pre_ci.sh` pass on this wave scope
- Task-specific Wave-4 verifier evidence generated and schema-validated
- SQLSTATE map and ADR baseline governance updates committed with migration
