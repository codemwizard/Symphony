---
exception_id: EXC-1288
inv_scope: change-rule
expiry: 2026-12-31
closed_at: 2026-03-21
follow_up_ticket: TSK-HARD-WAVE6-EXCEPTION
reason: Auto-generated: structural change detected; invariants linkage missing in commit.
author: system
created_at: 2026-03-05
---

# Exception: ddl structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

Wave-6 introduces required resilience/privacy DDL and baseline updates for hardening closeout.
Invariant linkage and verifier evidence are included in this commit under the Wave-6 task pack artifacts.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: ddl
reason_types: ddl, migration_file_added_or_deleted, security

Matched files:
- schema/migrations/0067_hard_wave6_operational_resilience_and_privacy.sql

Top matches:
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- ddl | schema/baseline.sql | +: CREATE TABLE public.artifact_signing_batch_items (
- ddl | schema/baseline.sql | +: CONSTRAINT artifact_signing_batch_items_leaf_index_check CHECK ((leaf_index >= 0))
- ddl | schema/baseline.sql | +: CREATE TABLE public.artifact_signing_batches (
- ddl | schema/baseline.sql | +: CONSTRAINT artifact_signing_batches_total_artifacts_check CHECK ((total_artifacts > 0))
- ddl | schema/baseline.sql | +: CREATE TABLE public.audit_tamper_evident_chains (
- ddl | schema/baseline.sql | +: CREATE TABLE public.boz_operational_scenario_runs (
- ddl | schema/baseline.sql | +: CONSTRAINT boz_operational_scenario_runs_outcome_check CHECK ((outcome = ANY (ARRAY['PASS'::text, 'FAIL'::text])))
- ddl | schema/baseline.sql | +: CREATE TABLE public.global_rate_limit_policies (
- ddl | schema/baseline.sql | +: CONSTRAINT global_rate_limit_policies_interval_seconds_check CHECK ((interval_seconds > 0)),
- ddl | schema/baseline.sql | +: CONSTRAINT global_rate_limit_policies_max_requests_check CHECK ((max_requests > 0))
- ddl | schema/baseline.sql | +: CREATE TABLE public.hsm_fail_closed_events (
- ddl | schema/baseline.sql | +: CREATE TABLE public.penalty_defense_packs (
- ddl | schema/baseline.sql | +: CREATE TABLE public.pii_erased_subject_placeholders (
- ddl | schema/baseline.sql | +: CREATE TABLE public.pii_erasure_journal (
- ddl | schema/baseline.sql | +: CONSTRAINT pii_erasure_journal_status_check CHECK ((status = ANY (ARRAY['REQUESTED'::text, 'APPROVED'::text, 'COMPLETED'::text, 'FAILED'::text])))
- ddl | schema/baseline.sql | +: CREATE TABLE public.pii_tokenization_registry (
- ddl | schema/baseline.sql | +: CREATE TABLE public.redaction_audit_events (

## Mitigation

DDL is forward-only, verified via dedicated task scripts plus full `scripts/dev/pre_ci.sh`.
Fail-closed SQLSTATE mappings and evidence schemas are updated in the same change set.
