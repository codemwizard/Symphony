---
exception_id: EXC-902
inv_scope: change-rule
expiry: 2026-12-31
follow_up_ticket: TSK-P0-KYC-002
reason: Structural DDL was introduced under task-governed migration and verifier wiring; invariants linkage is tracked via this bounded exception.
author: codex
created_at: 2026-02-23
closed_at: 2026-03-09
---

# Exception: ddl structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

TSK-P0-KYC-002 adds a forward-only migration and fail-closed verifier for the Phase-0 KYC
structural hook. The task already records invariant ownership in task metadata and Phase-0
contract evidence wiring, and this exception keeps commit-time policy explicit without
broadening scope.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: ddl
reason_types: ddl, migration_file_added_or_deleted

Matched files:
- schema/migrations/0042_kyc_verification_records_hook.sql

Top matches:
- ddl | docs/tasks/phase1_prompts.md | -: REFERENCES members(id)
- ddl | docs/tasks/phase1_prompts.md | +: REFERENCES public.tenant_members(member_id)
- ddl | schema/baseline.sql | +: CREATE TABLE public.kyc_verification_records (
- ddl | schema/baseline.sql | +: CONSTRAINT kyc_verification_records_retention_class_check CHECK ((retention_class = 'FIC_AML_CUSTOMER_ID'::text))
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.kyc_verification_records
- ddl | schema/baseline.sql | +: ADD CONSTRAINT kyc_verification_records_pkey PRIMARY KEY (id);
- ddl | schema/baseline.sql | +: CREATE INDEX kyc_verification_jurisdiction_outcome_idx ON public.kyc_verification_records USING btree (jurisdiction_code, outcome) WHERE (outcome IS NOT NULL);
- ddl | schema/baseline.sql | +: CREATE INDEX kyc_verification_member_idx ON public.kyc_verification_records USING btree (member_id, anchored_at DESC);
- ddl | schema/baseline.sql | +: CREATE INDEX kyc_verification_provider_idx ON public.kyc_verification_records USING btree (provider_id) WHERE (provider_id IS NOT NULL);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.kyc_verification_records
- ddl | schema/baseline.sql | +: ADD CONSTRAINT kyc_verification_records_member_id_fkey FOREIGN KEY (member_id) REFERENCES public.tenant_members(member_id) ON DELETE RESTRICT;
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.kyc_verification_records
- ddl | schema/baseline.sql | +: ADD CONSTRAINT kyc_verification_records_provider_id_fkey FOREIGN KEY (provider_id) REFERENCES public.kyc_provider_registry(id) ON DELETE RESTRICT;
- ddl | schema/baselines/2026-02-23/0001_baseline.sql | +: CREATE TABLE public.kyc_verification_records (
- ddl | schema/baselines/2026-02-23/0001_baseline.sql | +: CONSTRAINT kyc_verification_records_retention_class_check CHECK ((retention_class = 'FIC_AML_CUSTOMER_ID'::text))
- ddl | schema/baselines/2026-02-23/0001_baseline.sql | +: ALTER TABLE ONLY public.kyc_verification_records
- ddl | schema/baselines/2026-02-23/0001_baseline.sql | +: ADD CONSTRAINT kyc_verification_records_pkey PRIMARY KEY (id);
- ddl | schema/baselines/2026-02-23/0001_baseline.sql | +: CREATE INDEX kyc_verification_jurisdiction_outcome_idx ON public.kyc_verification_records USING btree (jurisdiction_code, outcome) WHERE (outcome IS NOT NULL);
- ddl | schema/baselines/2026-02-23/0001_baseline.sql | +: CREATE INDEX kyc_verification_member_idx ON public.kyc_verification_records USING btree (member_id, anchored_at DESC);
- ddl | schema/baselines/2026-02-23/0001_baseline.sql | +: CREATE INDEX kyc_verification_provider_idx ON public.kyc_verification_records USING btree (provider_id) WHERE (provider_id IS NOT NULL);
- ddl | schema/baselines/2026-02-23/0001_baseline.sql | +: ALTER TABLE ONLY public.kyc_verification_records
- ddl | schema/baselines/2026-02-23/0001_baseline.sql | +: ADD CONSTRAINT kyc_verification_records_member_id_fkey FOREIGN KEY (member_id) REFERENCES public.tenant_members(member_id) ON DELETE RESTRICT;
- ddl | schema/baselines/2026-02-23/0001_baseline.sql | +: ALTER TABLE ONLY public.kyc_verification_records
- ddl | schema/baselines/2026-02-23/0001_baseline.sql | +: ADD CONSTRAINT kyc_verification_records_provider_id_fkey FOREIGN KEY (provider_id) REFERENCES public.kyc_provider_registry(id) ON DELETE RESTRICT;
- ddl | schema/baselines/2026-02-23/baseline.normalized.sql | +: ADD CONSTRAINT kyc_verification_records_member_id_fkey FOREIGN KEY (member_id) REFERENCES public.tenant_members(member_id) ON DELETE RESTRICT;

## Mitigation

Fail-closed verifier `scripts/db/verify_kyc_verification_records_hook.sh` is wired in
`scripts/dev/pre_ci.sh`, evidence is contract-required at
`evidence/phase0/TSK-P0-KYC-002.json`, and full `RUN_PHASE1_GATES=1 scripts/dev/pre_ci.sh`
passed on this branch.
