---
exception_id: EXC-903
inv_scope: change-rule
expiry: 2026-04-15
follow_up_ticket: TSK-P0-LEVY-004
reason: Auto-generated: structural change detected; invariants linkage missing in commit.
author: system
created_at: 2026-02-22
---

# Exception: ddl structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

TSK-P0-LEVY-004 introduces a forward-only schema migration and baseline refresh for a
new Phase-0 structural hook (`levy_remittance_periods`). The commit touches DDL
and baseline files, so the structural change detector requires explicit exception
metadata.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: ddl
reason_types: ddl, migration_file_added_or_deleted

Matched files:
- schema/migrations/0038_levy_remittance_periods_hook.sql

Top matches:
- ddl | schema/baseline.sql | +: CREATE TABLE public.levy_remittance_periods (
- ddl | schema/baseline.sql | +: CONSTRAINT levy_remittance_periods_check CHECK ((period_end >= period_start)),
- ddl | schema/baseline.sql | +: CONSTRAINT levy_remittance_periods_check1 CHECK (((filing_deadline IS NULL) OR (filing_deadline >= period_end))),
- ddl | schema/baseline.sql | +: CONSTRAINT levy_remittance_periods_period_code_check CHECK ((period_code ~ '^\\d{4}-\\d{2}$'::text))
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.levy_remittance_periods
- ddl | schema/baseline.sql | +: ADD CONSTRAINT levy_periods_unique_period_jurisdiction UNIQUE (period_code, jurisdiction_code);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.levy_remittance_periods
- ddl | schema/baseline.sql | +: ADD CONSTRAINT levy_remittance_periods_pkey PRIMARY KEY (id);
- ddl | schema/baseline.sql | +: CREATE INDEX levy_periods_jurisdiction_idx ON public.levy_remittance_periods USING btree (jurisdiction_code, period_start DESC);
- ddl | schema/baseline.sql | +: CREATE INDEX levy_periods_status_idx ON public.levy_remittance_periods USING btree (period_status) WHERE (period_status IS NOT NULL);
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: CREATE TABLE public.levy_remittance_periods (
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: CONSTRAINT levy_remittance_periods_check CHECK ((period_end >= period_start)),
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: CONSTRAINT levy_remittance_periods_check1 CHECK (((filing_deadline IS NULL) OR (filing_deadline >= period_end))),
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: CONSTRAINT levy_remittance_periods_period_code_check CHECK ((period_code ~ '^\\d{4}-\\d{2}$'::text))
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: ALTER TABLE ONLY public.levy_remittance_periods
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: ADD CONSTRAINT levy_periods_unique_period_jurisdiction UNIQUE (period_code, jurisdiction_code);
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: ALTER TABLE ONLY public.levy_remittance_periods
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: ADD CONSTRAINT levy_remittance_periods_pkey PRIMARY KEY (id);
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: CREATE INDEX levy_periods_jurisdiction_idx ON public.levy_remittance_periods USING btree (jurisdiction_code, period_start DESC);
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: CREATE INDEX levy_periods_status_idx ON public.levy_remittance_periods USING btree (period_status) WHERE (period_status IS NOT NULL);
- ddl | schema/baselines/2026-02-22/baseline.normalized.sql | +: ADD CONSTRAINT levy_periods_unique_period_jurisdiction UNIQUE (period_code, jurisdiction_code);
- ddl | schema/baselines/2026-02-22/baseline.normalized.sql | +: ADD CONSTRAINT levy_remittance_periods_pkey PRIMARY KEY (id);
- ddl | schema/baselines/2026-02-22/baseline.normalized.sql | +: CONSTRAINT levy_remittance_periods_check CHECK ((period_end >= period_start)),
- ddl | schema/baselines/2026-02-22/baseline.normalized.sql | +: CONSTRAINT levy_remittance_periods_check1 CHECK (((filing_deadline IS NULL) OR (filing_deadline >= period_end))),
- ddl | schema/baselines/2026-02-22/baseline.normalized.sql | +: CONSTRAINT levy_remittance_periods_period_code_check CHECK ((period_code ~ '^\\d{4}-\\d{2}$'::text))

## Mitigation

- `RUN_PHASE1_GATES=1 scripts/dev/pre_ci.sh` is required before push for this task.
- The new verifier `scripts/db/verify_levy_remittance_periods_hook.sh` is wired in
  `scripts/dev/pre_ci.sh` and fails closed on schema/runtime drift.
