---
exception_id: EXC-906
inv_scope: change-rule
expiry: 2026-03-10
follow_up_ticket: TSK-P1-HIER-005
reason: Auto-generated: structural change detected; invariants linkage missing in commit.
author: system
created_at: 2026-02-24
closed_at: 2026-03-10
---

# Exception: security structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

This commit introduces `TSK-P1-HIER-005` structural hierarchy verification
controls in a new migration and verifier script. The preflight detector flags
security-typed structural changes because the function is `SECURITY DEFINER`
with explicit `search_path` hardening. The task is approved, verifier-backed,
and run through full `pre_ci`, but local Rule-1 preflight still requires the
exception artifact.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: security
reason_types: migration_file_added_or_deleted, security

Matched files:
- schema/migrations/0050_hier_005_instruction_hierarchy_verifier.sql
- scripts/db/verify_tsk_p1_hier_005.sh

Top matches:
- security | evidence/phase1/tsk_p1_hier_005__member_devices_tenant_safe_reverse_lookup.json | +: "detail": "verify_instruction_hierarchy is SECURITY DEFINER"
- security | evidence/phase1/tsk_p1_hier_005__member_devices_tenant_safe_reverse_lookup.json | +: "detail": "verify_instruction_hierarchy uses SET search_path = pg_catalog, public"
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baselines/2026-02-24/0001_baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baselines/2026-02-24/0001_baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baselines/2026-02-24/baseline.normalized.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baselines/2026-02-24/baseline.normalized.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baselines/current/0001_baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baselines/current/0001_baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/migrations/0050_hier_005_instruction_hierarchy_verifier.sql | +: SECURITY DEFINER
- security | schema/migrations/0050_hier_005_instruction_hierarchy_verifier.sql | +: SET search_path = pg_catalog, public
- security | scripts/db/verify_tsk_p1_hier_005.sh | +: run_bool_query "function_security_definer" "verify_instruction_hierarchy is SECURITY DEFINER" \
- security | scripts/db/verify_tsk_p1_hier_005.sh | +: run_bool_query "function_search_path_hardening" "verify_instruction_hierarchy uses SET search_path = pg_catalog, public" \
- security | scripts/db/verify_tsk_p1_hier_005.sh | +: "SELECT EXISTS(SELECT 1 FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace WHERE n.nspname='public' AND p.proname='verify_instruction_hierarchy' AND array_to_string(p.proconfig,',') LIKE '%search_path=pg_catalog, public%');"
- migration_file_added_or_deleted | schema/migrations/0050_hier_005_instruction_hierarchy_verifier.sql | +: (migration file added or deleted)

## Mitigation

- `verify_instruction_hierarchy()` is hardened with `SECURITY DEFINER` and
  `SET search_path = pg_catalog, public`.
- Task verifier `scripts/db/verify_tsk_p1_hier_005.sh` validates function
  hardening, deterministic SQLSTATE behavior, and emits task evidence.
- Full gate chain `RUN_PHASE1_GATES=1 scripts/dev/pre_ci.sh` passed after
  migration + baseline governance updates.
