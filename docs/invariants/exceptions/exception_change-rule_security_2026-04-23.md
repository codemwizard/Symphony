---
exception_id: EXC-025
inv_scope: change-rule
expiry: 2026-05-07
follow_up_ticket: TSK-P2-PREAUTH-005
reason: Bug fix for migration ordering issue. Migration 0122 (data_authority_triggers) incorrectly referenced state_transitions table which doesn't exist until migration 0137. Moved trigger functions and attachments from 0122 to 0137 to fix pre_ci phase1_db_verifiers failure.
author: system
created_at: 2026-04-23
---

# Exception: security structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

Bug fix for migration ordering issue. Migration 0122 (data_authority_triggers) incorrectly referenced state_transitions table which doesn't exist until migration 0137. This caused pre_ci phase1_db_verifiers to fail with "relation state_transitions does not exist". The fix removes trigger functions and attachments from 0122 and adds them to 0137 where the table is created. This is a bug fix, not a feature change, so invariants linkage is deferred to Stage B approval.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: security
reason_types: security

Matched files:
- schema/migrations/0122_create_data_authority_triggers.sql
- schema/migrations/0137_create_state_transitions.sql

Top matches:
- security | schema/migrations/0122_create_data_authority_triggers.sql | -: $$ LANGUAGE plpgsql SECURITY DEFINER SET search_path = pg_catalog, public;
- security | schema/migrations/0122_create_data_authority_triggers.sql | -: $$ LANGUAGE plpgsql SECURITY DEFINER SET search_path = pg_catalog, public;
- security | schema/migrations/0137_create_state_transitions.sql | +: $$ LANGUAGE plpgsql SECURITY DEFINER SET search_path = pg_catalog, public;
- security | schema/migrations/0137_create_state_transitions.sql | +: $$ LANGUAGE plpgsql SECURITY DEFINER SET search_path = pg_catalog, public;

## Mitigation

- This is a bug fix for a migration ordering issue discovered during pre_ci verification
- The change preserves the same trigger logic (enforce_state_transition_authority and upgrade_authority_on_execution_binding)
- The triggers are now defined in the correct migration (0137 where the table is created)
- SECURITY DEFINER hardening is preserved (SET search_path = pg_catalog, public)
- Invariants linkage will be added during Stage B approval after PR review
