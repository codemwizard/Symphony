---
exception_id: EXC-20260226-REG003
inv_scope: change-rule
expiry: 2026-03-26
follow_up_ticket: TSK-P1-REG-003
reason: DDL addition for regulatory incident workflow is introduced with explicit Phase-1 contract/allowlist/registry linkage in this task commit.
author: orchestrator
created_at: 2026-02-26
---

# Exception: ddl structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

Pre-commit structural preflight flagged the migration/baseline update before linkage metadata was staged.  
This exception documents the transitional preflight event while the same commit includes explicit invariant linkage updates.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: ddl
reason_types: ddl, migration_file_added_or_deleted

Matched files:
- schema/migrations/0060_reg_003_incident_workflow_48h_export.sql

Top matches:
- ddl | schema/baseline.sql | +: CREATE TABLE public.incident_events (
- ddl | schema/baseline.sql | +: CREATE TABLE public.regulatory_incidents (
- ddl | schema/baseline.sql | +: CONSTRAINT regulatory_incidents_severity_check CHECK ((severity = ANY (ARRAY['LOW'::text, 'MEDIUM'::text, 'HIGH'::text, 'CRITICAL'::text]))),
- ddl | schema/baseline.sql | +: CONSTRAINT regulatory_incidents_status_check CHECK ((status = ANY (ARRAY['OPEN'::text, 'UNDER_INVESTIGATION'::text, 'REPORTED'::text, 'CLOSED'::text])))
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.incident_events
- ddl | schema/baseline.sql | +: ADD CONSTRAINT incident_events_pkey PRIMARY KEY (incident_event_id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.regulatory_incidents
- ddl | schema/baseline.sql | +: ADD CONSTRAINT regulatory_incidents_pkey PRIMARY KEY (incident_id);
- ddl | schema/baseline.sql | +: CREATE INDEX ix_incident_events_incident_created ON public.incident_events USING btree (incident_id, created_at);
- ddl | schema/baseline.sql | +: CREATE INDEX ix_regulatory_incidents_tenant_detected ON public.regulatory_incidents USING btree (tenant_id, detected_at DESC);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.incident_events
- ddl | schema/baseline.sql | +: ADD CONSTRAINT incident_events_incident_id_fkey FOREIGN KEY (incident_id) REFERENCES public.regulatory_incidents(incident_id) ON DELETE CASCADE;
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.regulatory_incidents
- ddl | schema/baseline.sql | +: ADD CONSTRAINT regulatory_incidents_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(tenant_id) ON DELETE RESTRICT;
- ddl | schema/baselines/2026-02-25/0001_baseline.sql | +: CREATE TABLE public.incident_events (
- ddl | schema/baselines/2026-02-25/0001_baseline.sql | +: CREATE TABLE public.regulatory_incidents (
- ddl | schema/baselines/2026-02-25/0001_baseline.sql | +: CONSTRAINT regulatory_incidents_severity_check CHECK ((severity = ANY (ARRAY['LOW'::text, 'MEDIUM'::text, 'HIGH'::text, 'CRITICAL'::text]))),
- ddl | schema/baselines/2026-02-25/0001_baseline.sql | +: CONSTRAINT regulatory_incidents_status_check CHECK ((status = ANY (ARRAY['OPEN'::text, 'UNDER_INVESTIGATION'::text, 'REPORTED'::text, 'CLOSED'::text])))
- ddl | schema/baselines/2026-02-25/0001_baseline.sql | +: ALTER TABLE ONLY public.incident_events
- ddl | schema/baselines/2026-02-25/0001_baseline.sql | +: ADD CONSTRAINT incident_events_pkey PRIMARY KEY (incident_event_id);
- ddl | schema/baselines/2026-02-25/0001_baseline.sql | +: ALTER TABLE ONLY public.regulatory_incidents
- ddl | schema/baselines/2026-02-25/0001_baseline.sql | +: ADD CONSTRAINT regulatory_incidents_pkey PRIMARY KEY (incident_id);
- ddl | schema/baselines/2026-02-25/0001_baseline.sql | +: CREATE INDEX ix_incident_events_incident_created ON public.incident_events USING btree (incident_id, created_at);
- ddl | schema/baselines/2026-02-25/0001_baseline.sql | +: CREATE INDEX ix_regulatory_incidents_tenant_detected ON public.regulatory_incidents USING btree (tenant_id, detected_at DESC);
- ddl | schema/baselines/2026-02-25/0001_baseline.sql | +: ALTER TABLE ONLY public.incident_events

## Mitigation

- `docs/PHASE1/phase1_contract.yml` adds INV-119 linkage for `verify_reg_003_incident_48h_export.sh`.
- `docs/operations/VERIFIER_EVIDENCE_REGISTRY.yml` and `SEMANTIC_INTEGRITY_ALLOWLIST.yml` are updated in the same commit.
- `scripts/dev/pre_ci.sh` now runs the new verifier before Phase-1 contract evidence parity checks.
