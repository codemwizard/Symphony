---
exception_id: EXC-1287
inv_scope: change-rule
expiry: 2026-12-31
closed_at: 2026-03-21
follow_up_ticket: TSK-HARD-WAVE5-EXCEPTION
reason: Auto-generated: structural change detected; invariants linkage missing in commit.
author: system
created_at: 2026-03-05
---

# Exception: ddl structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

Wave-5 introduces required archive/merkle/replay DDL and baseline updates.
Invariant linkage is tracked in the hardening task pack and verifier evidence in this commit.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: ddl
reason_types: ddl, migration_file_added_or_deleted, security

Matched files:
- schema/migrations/0066_hard_wave5_archive_merkle_and_replay.sql

Top matches:
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- ddl | schema/baseline.sql | +: CREATE TABLE public.anchor_backfill_jobs (
- ddl | schema/baseline.sql | +: CONSTRAINT anchor_backfill_jobs_status_check CHECK ((status = ANY (ARRAY['STARTED'::text, 'COMPLETED'::text, 'FAILED'::text])))
- ddl | schema/baseline.sql | +: CREATE TABLE public.archive_verification_runs (
- ddl | schema/baseline.sql | +: CONSTRAINT archive_verification_runs_outcome_check CHECK ((outcome = ANY (ARRAY['PASS'::text, 'FAIL'::text]))),
- ddl | schema/baseline.sql | +: CONSTRAINT archive_verification_runs_years_covered_check CHECK ((years_covered >= 1))
- ddl | schema/baseline.sql | +: CREATE TABLE public.canonicalization_archive_snapshots (
- ddl | schema/baseline.sql | +: CREATE TABLE public.canonicalization_registry (
- ddl | schema/baseline.sql | +: CREATE TABLE public.proof_pack_batch_leaves (
- ddl | schema/baseline.sql | +: CONSTRAINT proof_pack_batch_leaves_leaf_index_check CHECK ((leaf_index >= 0))
- ddl | schema/baseline.sql | +: CREATE TABLE public.proof_pack_batches (
- ddl | schema/baseline.sql | +: CONSTRAINT proof_pack_batches_leaf_count_check CHECK ((leaf_count > 0))
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.anchor_backfill_jobs
- ddl | schema/baseline.sql | +: ADD CONSTRAINT anchor_backfill_jobs_pkey PRIMARY KEY (job_id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.archive_verification_runs
- ddl | schema/baseline.sql | +: ADD CONSTRAINT archive_verification_runs_pkey PRIMARY KEY (run_id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.canonicalization_archive_snapshots
- ddl | schema/baseline.sql | +: ADD CONSTRAINT canonicalization_archive_snap_canonicalization_version_snap_key UNIQUE (canonicalization_version, snapshot_sha256);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.canonicalization_archive_snapshots
- ddl | schema/baseline.sql | +: ADD CONSTRAINT canonicalization_archive_snapshots_pkey PRIMARY KEY (snapshot_id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.canonicalization_registry
- ddl | schema/baseline.sql | +: ADD CONSTRAINT canonicalization_registry_pkey PRIMARY KEY (canonicalization_version);

## Mitigation

DDL is forward-only, guarded by verifier scripts and full `scripts/dev/pre_ci.sh` execution.
No privilege broadening was introduced; SQLSTATE map and evidence schemas were updated in lockstep.
