---
exception_id: EXC-20260312-INT004-SEC
inv_scope: change-rule
expiry: 2026-12-31
follow_up_ticket: TSK-P1-INT-004
reason: Auto-generated: structural change detected; invariants linkage missing in commit.
author: system
created_at: 2026-03-12
---

# Exception: security structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

Wave C carries forward the INT-004 acknowledgement-gap migration and adds the missing settlement-write trigger so `SETTLED` inserts fail closed until the instruction is explicitly `ACKNOWLEDGED`. The same batch also keeps SECURITY DEFINER hardening and revoke-first posture intact.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: security
reason_types: security

Matched files:
- schema/migrations/0073_int_004_ack_gap_controls.sql

Top matches:
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/migrations/0073_int_004_ack_gap_controls.sql | +: SECURITY DEFINER
- security | schema/migrations/0073_int_004_ack_gap_controls.sql | +: SET search_path = pg_catalog, public
- security | schema/migrations/0073_int_004_ack_gap_controls.sql | +: REVOKE ALL ON FUNCTION public.enforce_settlement_acknowledgement() FROM PUBLIC;

## Mitigation

- `public.enforce_settlement_acknowledgement()` is SECURITY DEFINER with fixed `search_path`.
- `REVOKE ALL` remains applied to the trigger function and guard function.
- `scripts/audit/verify_tsk_p1_int_004.sh` now checks that the trigger is present and calls `guard_settlement_requires_acknowledgement`.
- `scripts/db/tests/test_instruction_finality.sh` now includes a fail-closed missing-ack settlement case for environments where `DATABASE_URL` is available.
