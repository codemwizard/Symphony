---
exception_id: EXC-902
inv_scope: change-rule
expiry: 2026-03-19
closed_at: 2026-03-21
follow_up_ticket: WAVE1-HARDENING-CLOSEOUT
reason: Auto-generated: structural change detected; invariants linkage missing in commit.
author: system
created_at: 2026-03-05
---

# Exception: ddl structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

Wave-1 hardening tasks 013-017, 094, 101, and 013B introduce new DDL objects in
`0062_hard_wave1_runtime_controls.sql`. The change is intentional and required to
provide fail-closed database primitives for effect sealing, conflict containment,
quarantine, circuit-breaker suspension, offline gating, and replay rejection.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: ddl
reason_types: ddl, migration_file_added_or_deleted, security

Matched files:
- schema/migrations/0062_hard_wave1_runtime_controls.sql

Top matches:
- ddl | schema/baseline.sql | +: CREATE TYPE public.finality_resolution_state_enum AS ENUM (
- ddl | schema/baseline.sql | +: CREATE TYPE public.finality_signal_status_enum AS ENUM (
- ddl | schema/baseline.sql | +: CREATE TYPE public.orphan_classification_enum AS ENUM (
- ddl | schema/baseline.sql | +: CREATE TYPE public.quarantine_classification_enum AS ENUM (
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- ddl | schema/baseline.sql | +: CREATE TABLE public.adapter_circuit_breakers (

## Mitigation

Mitigations:
- All new SECURITY DEFINER functions use `SET search_path = pg_catalog, public`.
- SQLSTATE codes are registered in `docs/contracts/sqlstate_map.yml`.
- Full gate run with `RUN_PHASE1_GATES=1 scripts/dev/pre_ci.sh` is required before closeout.
