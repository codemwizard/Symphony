---
exception_id: EXC-DRD-008
inv_scope: change-rule
expiry: 2026-05-17
follow_up_ticket: TSK-OPS-DRD-008
reason: Migration 0116 idempotency fix — IF NOT EXISTS guards added to unblock pre_ci DB parity probe.
author: security_guardian
created_at: 2026-04-17
---

# Exception: ddl structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

Migration `0116_create_interpretation_packs.sql` was modified to add `IF NOT EXISTS` guards.
This is a bug fix (DRD Lite L1), not a new migration. The DDL semantics are unchanged —
only idempotency is added so the migration replays without error during CI DB parity probes.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: ddl
reason_types: ddl, migration_file_added_or_deleted, security

Matched files:
- schema/migrations/0116_create_interpretation_packs.sql

Top matches:
- ddl | docs/plans/phase1/TSK-OPS-DRD-008/DRD.md | +: Migration `0116_create_interpretation_packs.sql` (created by TSK-P2-PREAUTH-001-01) used bare `CREATE TABLE` and `CREATE INDEX` without `IF NOT EXISTS` guards. When the CI DB parity probe replayed all migrations against both local and ephemeral databases, the migration failed with `ERROR: relation "
- ddl | docs/plans/phase1/TSK-OPS-DRD-008/DRD.md | +: - Confirmed: Migration `0116_create_interpretation_packs.sql` lacked `IF NOT EXISTS` on `CREATE TABLE` (line 4) and two `CREATE INDEX` statements (lines 15, 18). All Symphony migrations must be idempotently replayable per forward-only migration policy.
- ddl | docs/plans/phase1/TSK-OPS-DRD-008/DRD.md | +: - Line 4: `CREATE TABLE` → `CREATE TABLE IF NOT EXISTS`
- ddl | docs/plans/phase1/TSK-OPS-DRD-008/DRD.md | +: - Line 15: `CREATE INDEX` → `CREATE INDEX IF NOT EXISTS`
- ddl | docs/plans/phase1/TSK-OPS-DRD-008/DRD.md | +: - Line 18: `CREATE INDEX` → `CREATE INDEX IF NOT EXISTS`
- ddl | schema/migrations/0116_create_interpretation_packs.sql | +: CREATE TABLE IF NOT EXISTS interpretation_packs (
- ddl | schema/migrations/0116_create_interpretation_packs.sql | +: CREATE INDEX IF NOT EXISTS idx_interpretation_packs_project_time ON interpretation_packs (project_id, effective_from DESC, effective_to);
- ddl | schema/migrations/0116_create_interpretation_packs.sql | +: CREATE INDEX IF NOT EXISTS idx_interpretation_packs_code ON interpretation_packs (interpretation_pack_code);
- security | schema/migrations/0116_create_interpretation_packs.sql | +: -- Function: resolve_interpretation_pack() with exact signature and SECURITY DEFINER
- security | schema/migrations/0116_create_interpretation_packs.sql | +: SECURITY DEFINER
- security | schema/migrations/0116_create_interpretation_packs.sql | +: SET search_path = pg_catalog, public
- migration_file_added_or_deleted | schema/migrations/0116_create_interpretation_packs.sql | +: (migration file added or deleted)

## Mitigation

- DRD Lite filed: `docs/plans/phase1/TSK-OPS-DRD-008/DRD.md`
- `IF NOT EXISTS` is the standard idempotency pattern used by all other Symphony migrations
- No schema change: the DDL creates the same objects, it just skips if they already exist
- Forward-only policy preserved: no applied migration was edited (0116 was failing on replay)
