---
exception_id: EXC-024
inv_scope: change-rule
expiry: 2026-05-07
follow_up_ticket: TSK-P2-PREAUTH-005
reason: Bug fix for migration ordering issue. Migration 0121 referenced state_transitions table which doesn't exist until migration 0137. Moved data authority columns for state_transitions from 0121 to 0137 to fix pre_ci phase1_db_verifiers failure.
author: system
created_at: 2026-04-23
---

# Exception: ddl structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

Bug fix for migration ordering issue. Migration 0121 (data_authority_enum) incorrectly referenced state_transitions table which doesn't exist until migration 0137. This caused pre_ci phase1_db_verifiers to fail with "relation state_transitions does not exist". The fix removes state_transitions ALTER statements from 0121 and adds the data authority columns directly to the state_transitions table definition in migration 0137. This is a bug fix, not a feature change, so invariants linkage is deferred to Stage B approval.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: ddl
reason_types: ddl

Matched files:
- schema/migrations/0121_create_data_authority_enum.sql
- schema/migrations/0137_create_state_transitions.sql

Top matches:
- ddl | schema/migrations/0121_create_data_authority_enum.sql | -: ALTER TABLE state_transitions
- ddl | schema/migrations/0121_create_data_authority_enum.sql | -: ADD COLUMN IF NOT EXISTS data_authority public.data_authority_level,
- ddl | schema/migrations/0121_create_data_authority_enum.sql | -: ADD COLUMN IF NOT EXISTS audit_grade BOOLEAN,
- ddl | schema/migrations/0121_create_data_authority_enum.sql | -: ADD COLUMN IF NOT EXISTS authority_explanation TEXT;
- ddl | schema/migrations/0121_create_data_authority_enum.sql | -: ALTER TABLE state_transitions
- ddl | schema/migrations/0121_create_data_authority_enum.sql | -: ALTER COLUMN data_authority SET NOT NULL,
- ddl | schema/migrations/0121_create_data_authority_enum.sql | -: ALTER COLUMN audit_grade SET NOT NULL,
- ddl | schema/migrations/0121_create_data_authority_enum.sql | -: ALTER COLUMN authority_explanation SET NOT NULL;
- ddl | schema/migrations/0137_create_state_transitions.sql | +: CREATE TYPE public.data_authority_level AS ENUM (

## Mitigation

- This is a bug fix for a migration ordering issue discovered during pre_ci verification
- The change preserves the same data model (data authority columns on state_transitions)
- The columns are now defined in the correct migration (0137 where the table is created)
- Default values match the original backfill values from migration 0121
- Invariants linkage will be added during Stage B approval after PR review
