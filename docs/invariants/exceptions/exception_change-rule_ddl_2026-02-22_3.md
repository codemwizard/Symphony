---
exception_id: EXC-904
inv_scope: change-rule
expiry: 2026-03-15
follow_up_ticket: TSK-P0-KYC-001
reason: Auto-generated: structural change detected; invariants linkage missing in commit.
author: system
created_at: 2026-02-22
---

# Exception: ddl structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

TSK-P0-KYC-001 introduces a forward-only schema migration and baseline refresh for a
new Phase-0 structural hook (`kyc_provider_registry`). The commit touches DDL
and baseline files, so the structural change detector requires explicit exception
metadata.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: ddl
reason_types: ddl, migration_file_added_or_deleted

Matched files:
- schema/migrations/0039_kyc_provider_registry_hook.sql

Top matches:
- ddl | schema/baseline.sql | +: CREATE TABLE public.kyc_provider_registry (
- ddl | schema/baseline.sql | +: CONSTRAINT kyc_provider_registry_check CHECK (((active_to IS NULL) OR (active_from IS NULL) OR (active_to >= active_from)))
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.kyc_provider_registry
- ddl | schema/baseline.sql | +: ADD CONSTRAINT kyc_provider_registry_pkey PRIMARY KEY (id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.kyc_provider_registry
- ddl | schema/baseline.sql | +: ADD CONSTRAINT kyc_provider_unique_active_per_jurisdiction UNIQUE (jurisdiction_code, provider_code);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.kyc_provider_registry
- ddl | schema/baseline.sql | +: ADD CONSTRAINT kyc_provider_unique_code UNIQUE (provider_code);
- ddl | schema/baseline.sql | +: CREATE UNIQUE INDEX kyc_provider_active_idx ON public.kyc_provider_registry USING btree (jurisdiction_code, provider_code) WHERE ((active_to IS NULL) AND (is_active IS NOT FALSE));
- ddl | schema/baseline.sql | +: CREATE INDEX kyc_provider_jurisdiction_idx ON public.kyc_provider_registry USING btree (jurisdiction_code, active_from DESC);
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: CREATE TABLE public.kyc_provider_registry (
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: CONSTRAINT kyc_provider_registry_check CHECK (((active_to IS NULL) OR (active_from IS NULL) OR (active_to >= active_from)))
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: ALTER TABLE ONLY public.kyc_provider_registry
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: ADD CONSTRAINT kyc_provider_registry_pkey PRIMARY KEY (id);
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: ALTER TABLE ONLY public.kyc_provider_registry
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: ADD CONSTRAINT kyc_provider_unique_active_per_jurisdiction UNIQUE (jurisdiction_code, provider_code);
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: ALTER TABLE ONLY public.kyc_provider_registry
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: ADD CONSTRAINT kyc_provider_unique_code UNIQUE (provider_code);
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: CREATE UNIQUE INDEX kyc_provider_active_idx ON public.kyc_provider_registry USING btree (jurisdiction_code, provider_code) WHERE ((active_to IS NULL) AND (is_active IS NOT FALSE));
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: CREATE INDEX kyc_provider_jurisdiction_idx ON public.kyc_provider_registry USING btree (jurisdiction_code, active_from DESC);
- ddl | schema/baselines/2026-02-22/baseline.normalized.sql | +: ADD CONSTRAINT kyc_provider_registry_pkey PRIMARY KEY (id);
- ddl | schema/baselines/2026-02-22/baseline.normalized.sql | +: ADD CONSTRAINT kyc_provider_unique_active_per_jurisdiction UNIQUE (jurisdiction_code, provider_code);
- ddl | schema/baselines/2026-02-22/baseline.normalized.sql | +: ADD CONSTRAINT kyc_provider_unique_code UNIQUE (provider_code);
- ddl | schema/baselines/2026-02-22/baseline.normalized.sql | +: CONSTRAINT kyc_provider_registry_check CHECK (((active_to IS NULL) OR (active_from IS NULL) OR (active_to >= active_from)))
- ddl | schema/baselines/2026-02-22/baseline.normalized.sql | +: ALTER TABLE ONLY public.kyc_provider_registry

## Mitigation

- `RUN_PHASE1_GATES=1 scripts/dev/pre_ci.sh` passes on this commit.
- The new verifier `scripts/db/verify_kyc_provider_registry_hook.sh` is wired in
  `scripts/dev/pre_ci.sh` and fails closed on schema/runtime drift.
