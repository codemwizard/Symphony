---
exception_id: EXC-905
inv_scope: change-rule
expiry: 2026-03-10
follow_up_ticket: TSK-P1-HIER-004
reason: Auto-generated: structural change detected; invariants linkage missing in commit.
author: system
created_at: 2026-02-24
---

# Exception: ddl structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

This commit introduces approved Phase-1 structural changes for `TSK-P1-HIER-004`
(`member_device_events` append-only enrollment event model) and corresponding
baseline snapshots. The change is task-scoped, verified, and references `INV-077`
and `INV-119`, but local preflight still requires an explicit exception artifact
for this structural delta.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: ddl
reason_types: ddl, migration_file_added_or_deleted, security

Matched files:
- schema/migrations/0049_hier_004_member_device_events_append_only.sql

Top matches:
- ddl | schema/baseline.sql | +: CREATE TABLE public.member_device_events (
- ddl | schema/baseline.sql | +: CONSTRAINT member_device_events_device_id_event_type_chk CHECK (((device_id IS NULL) = (event_type = ANY (ARRAY['UNREGISTERED_DEVICE'::text, 'REVOKED_DEVICE_ATTEMPT'::text])))),
- ddl | schema/baseline.sql | +: CONSTRAINT member_device_events_event_type_check CHECK ((event_type = ANY (ARRAY['ENROLLED_DEVICE'::text, 'UNREGISTERED_DEVICE'::text, 'REVOKED_DEVICE_ATTEMPT'::text])))
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.member_device_events
- ddl | schema/baseline.sql | +: ADD CONSTRAINT member_device_events_pkey PRIMARY KEY (event_id);
- ddl | schema/baseline.sql | +: CREATE INDEX idx_member_device_events_instruction ON public.member_device_events USING btree (instruction_id);
- ddl | schema/baseline.sql | +: CREATE INDEX idx_member_device_events_tenant_member_observed ON public.member_device_events USING btree (tenant_id, member_id, observed_at DESC);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.member_device_events
- ddl | schema/baseline.sql | +: ADD CONSTRAINT member_device_events_ingress_fk FOREIGN KEY (tenant_id, instruction_id) REFERENCES public.ingress_attestations(tenant_id, instruction_id) ON DELETE RESTRICT;
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.member_device_events
- ddl | schema/baseline.sql | +: ADD CONSTRAINT member_device_events_member_id_fkey FOREIGN KEY (member_id) REFERENCES public.members(member_id) ON DELETE RESTRICT;
- ddl | schema/baselines/2026-02-24/0001_baseline.sql | +: CREATE TABLE public.member_device_events (
- ddl | schema/baselines/2026-02-24/0001_baseline.sql | +: CONSTRAINT member_device_events_device_id_event_type_chk CHECK (((device_id IS NULL) = (event_type = ANY (ARRAY['UNREGISTERED_DEVICE'::text, 'REVOKED_DEVICE_ATTEMPT'::text])))),
- ddl | schema/baselines/2026-02-24/0001_baseline.sql | +: CONSTRAINT member_device_events_event_type_check CHECK ((event_type = ANY (ARRAY['ENROLLED_DEVICE'::text, 'UNREGISTERED_DEVICE'::text, 'REVOKED_DEVICE_ATTEMPT'::text])))
- ddl | schema/baselines/2026-02-24/0001_baseline.sql | +: ALTER TABLE ONLY public.member_device_events
- ddl | schema/baselines/2026-02-24/0001_baseline.sql | +: ADD CONSTRAINT member_device_events_pkey PRIMARY KEY (event_id);
- ddl | schema/baselines/2026-02-24/0001_baseline.sql | +: CREATE INDEX idx_member_device_events_instruction ON public.member_device_events USING btree (instruction_id);
- ddl | schema/baselines/2026-02-24/0001_baseline.sql | +: CREATE INDEX idx_member_device_events_tenant_member_observed ON public.member_device_events USING btree (tenant_id, member_id, observed_at DESC);
- ddl | schema/baselines/2026-02-24/0001_baseline.sql | +: ALTER TABLE ONLY public.member_device_events
- ddl | schema/baselines/2026-02-24/0001_baseline.sql | +: ADD CONSTRAINT member_device_events_ingress_fk FOREIGN KEY (tenant_id, instruction_id) REFERENCES public.ingress_attestations(tenant_id, instruction_id) ON DELETE RESTRICT;
- ddl | schema/baselines/2026-02-24/0001_baseline.sql | +: ALTER TABLE ONLY public.member_device_events
- ddl | schema/baselines/2026-02-24/0001_baseline.sql | +: ADD CONSTRAINT member_device_events_member_id_fkey FOREIGN KEY (member_id) REFERENCES public.members(member_id) ON DELETE RESTRICT;
- ddl | schema/baselines/2026-02-24/baseline.normalized.sql | +: ADD CONSTRAINT member_device_events_ingress_fk FOREIGN KEY (tenant_id, instruction_id) REFERENCES public.ingress_attestations(tenant_id, instruction_id) ON DELETE RESTRICT;
- ddl | schema/baselines/2026-02-24/baseline.normalized.sql | +: ADD CONSTRAINT member_device_events_member_id_fkey FOREIGN KEY (member_id) REFERENCES public.members(member_id) ON DELETE RESTRICT;
- ddl | schema/baselines/2026-02-24/baseline.normalized.sql | +: ADD CONSTRAINT member_device_events_pkey PRIMARY KEY (event_id);

## Mitigation

- Task verifier `scripts/db/verify_tsk_p1_hier_004.sh` passes and emits evidence at
  `evidence/phase1/tsk_p1_hier_004__person_model_explicit_enrollment.json`.
- Full parity gate `RUN_PHASE1_GATES=1 scripts/dev/pre_ci.sh` passed after this change.
- Structural documentation and baseline governance were updated in the same commit set
  (`THREAT_MODEL`, `COMPLIANCE_MAP`, `ADR-0010` baseline policy log).
