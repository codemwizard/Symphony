---
exception_id: EXC-902
inv_scope: change-rule
expiry: 2026-12-31
follow_up_ticket: TSK-P1-HIER-002
reason: Auto-generated: structural change detected; invariants linkage missing in commit.
author: system
created_at: 2026-02-24
closed_at: 2026-03-10
---

# Exception: migration_file_added_or_deleted structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

Baseline refresh for migration `0047_hier_002_programs_person_member_bridge.sql` is required to satisfy drift checks; this commit carries governed baseline snapshot updates plus ADR log entry.

## Evidence

structural_change: True
confidence_hint: 0.7
primary_reason: migration_file_added_or_deleted
reason_types: migration_file_added_or_deleted

Matched files:
- schema/baselines/2026-02-24/baseline.normalized.sql

Top matches:
- ddl | schema/baseline.sql | +: CREATE TABLE public.members (
- ddl | schema/baseline.sql | +: CONSTRAINT members_ceiling_amount_minor_check CHECK ((ceiling_amount_minor >= 0)),
- ddl | schema/baseline.sql | +: CONSTRAINT members_kyc_status_check CHECK ((kyc_status = ANY (ARRAY['PENDING'::text, 'VERIFIED'::text, 'REJECTED'::text]))),
- ddl | schema/baseline.sql | +: CONSTRAINT members_status_check CHECK ((status = ANY (ARRAY['ACTIVE'::text, 'SUSPENDED'::text, 'ARCHIVED'::text])))
- ddl | schema/baseline.sql | +: CREATE TABLE public.persons (
- ddl | schema/baseline.sql | +: CONSTRAINT persons_status_check CHECK ((status = ANY (ARRAY['ACTIVE'::text, 'INACTIVE'::text, 'SUSPENDED'::text])))
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.members
- ddl | schema/baseline.sql | +: ADD CONSTRAINT members_pkey PRIMARY KEY (member_id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.members
- ddl | schema/baseline.sql | +: ADD CONSTRAINT members_tenant_id_member_ref_hash_key UNIQUE (tenant_id, member_ref_hash);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.members
- ddl | schema/baseline.sql | +: ADD CONSTRAINT members_tenant_id_person_id_entity_id_key UNIQUE (tenant_id, person_id, entity_id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.persons
- ddl | schema/baseline.sql | +: ADD CONSTRAINT persons_pkey PRIMARY KEY (person_id);
- ddl | schema/baseline.sql | +: CREATE INDEX idx_members_entity_active ON public.members USING btree (tenant_id, entity_id, status) WHERE (status = 'ACTIVE'::text);
- ddl | schema/baseline.sql | +: CREATE INDEX idx_members_entity_member_ref_active ON public.members USING btree (tenant_id, entity_id, member_ref_hash) WHERE (status = 'ACTIVE'::text);
- ddl | schema/baseline.sql | +: CREATE INDEX idx_members_tenant_member ON public.members USING btree (tenant_id, member_id);
- ddl | schema/baseline.sql | +: CREATE INDEX idx_members_tenant_member_ref ON public.members USING btree (tenant_id, member_ref_hash);
- ddl | schema/baseline.sql | +: CREATE UNIQUE INDEX idx_persons_tenant_ref ON public.persons USING btree (tenant_id, person_ref_hash);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.members
- ddl | schema/baseline.sql | +: ADD CONSTRAINT members_entity_id_fkey FOREIGN KEY (entity_id) REFERENCES public.programs(program_id) ON DELETE RESTRICT;
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.members
- ddl | schema/baseline.sql | +: ADD CONSTRAINT members_person_id_fkey FOREIGN KEY (person_id) REFERENCES public.persons(person_id) ON DELETE RESTRICT;
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.members
- ddl | schema/baseline.sql | +: ADD CONSTRAINT members_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(tenant_id) ON DELETE RESTRICT;

## Mitigation

Fail-closed controls remain active via `verify_baseline_change_governance.sh`, `check_baseline_drift.sh`, and full `scripts/dev/pre_ci.sh` execution in the same branch.
