---
exception_id: EXC-1001
inv_scope: change-rule
expiry: 2026-12-31
follow_up_ticket: N/A
reason: Baseline regeneration using generate_baseline_snapshot.sh to sync with migration 0115. The script creates a new dated baseline directory (schema/baselines/2026-04-15/) which is detected as a structural change, but this is the standard baseline management process.
author: system
created_at: 2026-04-15
---

# Exception: migration_file_added_or_deleted structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

Baseline regeneration using generate_baseline_snapshot.sh to sync with migration 0115 (supplier_type column added to supplier_registry table). The script creates a new dated baseline directory (schema/baselines/2026-04-15/) which is detected as a structural change, but this is the standard baseline management process per ADR-0010.

## Evidence

structural_change: True
confidence_hint: 0.7
primary_reason: migration_file_added_or_deleted
reason_types: migration_file_added_or_deleted

Matched files:
- schema/baselines/2026-04-15/baseline.normalized.sql

Top matches:
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'

## Mitigation

Baseline regeneration is governed by ADR-0010 which requires both a migration change and ADR update when the baseline changes. This commit includes:
- Migration change: Updated migration 0115 with baseline sync comment
- ADR update: Updated ADR-0010 baseline log with regeneration entry
- The new baseline directory is the standard output of generate_baseline_snapshot.sh
