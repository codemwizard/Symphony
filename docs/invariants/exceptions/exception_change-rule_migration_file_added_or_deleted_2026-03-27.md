---
exception_id: EXC-997
inv_scope: change-rule
expiry: 2026-04-10
follow_up_ticket: TSK-P1-246
reason: Synchronizing native DB schemas and resolving orphaned schemas from wave 2.
author: system
created_at: 2026-03-27
---

# Exception: migration_file_added_or_deleted structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

Synchronizing the DB schema generated artifact (baseline.sql) after purging the rogue 0085 to 0094 sequence configurations and fixing native execution loops.

## Evidence

structural_change: True
confidence_hint: 0.7
primary_reason: migration_file_added_or_deleted
reason_types: migration_file_added_or_deleted

Matched files:
- schema/baselines/2026-03-27/baseline.normalized.sql

Top matches:
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- ddl | schema/baseline.sql | +: CREATE TABLE public.adapter_registrations (
- ddl | schema/baseline.sql | +: CONSTRAINT adapter_registrations_checklist_refs_check CHECK ((jsonb_typeof(checklist_refs) = 'array'::text)),
- ddl | schema/baseline.sql | +: CONSTRAINT adapter_registrations_entrypoint_refs_check CHECK ((jsonb_typeof(entrypoint_refs) = 'array'::text)),
- ddl | schema/baseline.sql | +: CONSTRAINT adapter_registrations_issuance_semantic_mode_check CHECK ((issuance_semantic_mode = ANY (ARRAY['STRICT'::text, 'LENIENT'::text, 'HYBRID'::text]))),
- ddl | schema/baseline.sql | +: CONSTRAINT adapter_registrations_jurisdiction_compatibility_check CHECK ((jsonb_typeof(jurisdiction_compatibility) = 'object'::text)),
- ddl | schema/baseline.sql | +: CONSTRAINT adapter_registrations_payload_schema_refs_check CHECK ((jsonb_typeof(payload_schema_refs) = 'array'::text)),
- ddl | schema/baseline.sql | +: CONSTRAINT adapter_registrations_retirement_semantic_mode_check CHECK ((retirement_semantic_mode = ANY (ARRAY['STRICT'::text, 'LENIENT'::text, 'HYBRID'::text])))
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.adapter_registrations FORCE ROW LEVEL SECURITY;
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.adapter_registrations
- ddl | schema/baseline.sql | +: ADD CONSTRAINT adapter_registration_unique UNIQUE (tenant_id, adapter_code, methodology_code, version_code);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.adapter_registrations
- ddl | schema/baseline.sql | +: ADD CONSTRAINT adapter_registrations_pkey PRIMARY KEY (adapter_registration_id);
- ddl | schema/baseline.sql | +: CREATE INDEX idx_adapter_registrations_adapter_code ON public.adapter_registrations USING btree (adapter_code);
- ddl | schema/baseline.sql | +: CREATE INDEX idx_adapter_registrations_methodology ON public.adapter_registrations USING btree (methodology_code, methodology_authority);
- ddl | schema/baseline.sql | +: CREATE INDEX idx_adapter_registrations_tenant_id ON public.adapter_registrations USING btree (tenant_id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.adapter_registrations
- ddl | schema/baseline.sql | +: ADD CONSTRAINT adapter_registrations_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(tenant_id) ON DELETE RESTRICT;
- ddl | schema/baseline.sql | +: ALTER TABLE public.adapter_registrations ENABLE ROW LEVEL SECURITY;
- ddl | schema/baseline.sql | +: CREATE POLICY rls_tenant_isolation_adapter_registrations ON public.adapter_registrations USING ((tenant_id = public.current_tenant_id_or_null())) WITH CHECK ((tenant_id = public.current_tenant_id_or_null()));
- security | schema/baselines/2026-03-27/0001_baseline.sql | +: SELECT pg_catalog.set_config('search_path', '', false);
- ddl | schema/baselines/2026-03-27/0001_baseline.sql | +: CREATE TYPE public.adjustment_state_enum AS ENUM (
- ddl | schema/baselines/2026-03-27/0001_baseline.sql | +: CREATE TYPE public.finality_resolution_state_enum AS ENUM (
- ddl | schema/baselines/2026-03-27/0001_baseline.sql | +: CREATE TYPE public.finality_signal_status_enum AS ENUM (

## Mitigation

All schema mutations strictly comply with deterministic compilation artifacts guaranteed natively by verify_invariants.sh.
