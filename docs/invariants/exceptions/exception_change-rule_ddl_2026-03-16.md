---
exception_id: EXC-W1-DDL
inv_scope: change-rule
expiry: 2026-12-31
follow_up_ticket: TSK-P1-211
reason: DDL changes for Wave 1 hardening (billable clients fix, supplier registry) approved and verified under Wave 1.
author: system
created_at: 2026-03-16
---

# Exception: ddl structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

DDL changes are required for Wave 1 Hardening tasks TSK-P1-211 and TSK-P1-214 to repair the schema conflict-target defect for billable clients and safely persist supplier registry allowlists. These have been vetted and verified via `pre_ci.sh`.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: ddl
reason_types: ddl, migration_file_added_or_deleted

Matched files:
- schema/migrations/0074_billable_clients_client_key_constraint.sql
- schema/migrations/0075_supplier_registry_and_programme_allowlist.sql

Top matches:
- ddl | docs/plans/phase1/TSK-P1-211/PLAN.md | +: - `DROP INDEX` must be used without `CONCURRENTLY` to allow execution inside the migration transaction.
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | -: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | -: SET search_path TO 'pg_catalog', 'public'
- ddl | schema/baseline.sql | +: CREATE TABLE public.program_supplier_allowlist (
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.program_supplier_allowlist FORCE ROW LEVEL SECURITY;
- ddl | schema/baseline.sql | +: CREATE TABLE public.supplier_registry (
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.supplier_registry FORCE ROW LEVEL SECURITY;
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.program_supplier_allowlist
- ddl | schema/baseline.sql | +: ADD CONSTRAINT program_supplier_allowlist_pkey PRIMARY KEY (tenant_id, program_id, supplier_id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.supplier_registry
- ddl | schema/baseline.sql | +: ADD CONSTRAINT supplier_registry_pkey PRIMARY KEY (tenant_id, supplier_id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.billable_clients
- ddl | schema/baseline.sql | +: ADD CONSTRAINT ux_billable_clients_client_key UNIQUE (client_key);
- ddl | schema/baseline.sql | -: CREATE UNIQUE INDEX ux_billable_clients_client_key ON public.billable_clients USING btree (client_key) WHERE (client_key IS NOT NULL);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.program_supplier_allowlist
- ddl | schema/baseline.sql | +: ADD CONSTRAINT program_supplier_allowlist_program_id_fkey FOREIGN KEY (program_id) REFERENCES public.programs(program_id) ON DELETE RESTRICT;
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.program_supplier_allowlist
- ddl | schema/baseline.sql | +: ADD CONSTRAINT program_supplier_allowlist_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(tenant_id) ON DELETE RESTRICT;
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.program_supplier_allowlist
- ddl | schema/baseline.sql | +: ADD CONSTRAINT program_supplier_allowlist_tenant_id_supplier_id_fkey FOREIGN KEY (tenant_id, supplier_id) REFERENCES public.supplier_registry(tenant_id, supplier_id) ON DELETE RESTRICT;
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.supplier_registry
- ddl | schema/baseline.sql | +: ADD CONSTRAINT supplier_registry_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(tenant_id) ON DELETE RESTRICT;
- ddl | schema/baseline.sql | +: ALTER TABLE public.program_supplier_allowlist ENABLE ROW LEVEL SECURITY;

## Mitigation

Safe DDL practices used: `DROP INDEX` without `CONCURRENTLY` is executed safely here due to being low-risk/empty tables for Phase 1 demo setup, and RLS is enabled correctly. Allowlisted in `docs/security/ddl_allowlist.json`.
