---
exception_id: EXC-20260312-INT004
inv_scope: change-rule
expiry: 2026-03-26
follow_up_ticket: TSK-P1-INT-004
reason: Auto-generated: structural change detected; invariants linkage missing in commit.
author: system
created_at: 2026-03-12
---

# Exception: ddl structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

Wave B introduces the INT-004 acknowledgement-gap migration and baseline refresh. The DDL is deliberate, forward-only, and linked to the Wave B task pack and verifier set in this branch.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: ddl
reason_types: ddl, migration_file_added_or_deleted, security

Matched files:
- schema/migrations/0073_int_004_ack_gap_controls.sql

Top matches:
- ddl | docs/security/SOVEREIGN_VPC_POSTURE.md | +: * Archived evidence may move to lower-cost storage only after archival eligibility is met and manifest/hash references are recorded.
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- ddl | schema/baseline.sql | -: CONSTRAINT supervisor_approval_queue_status_check CHECK ((status = ANY (ARRAY['PENDING_SUPERVISOR_APPROVAL'::text, 'APPROVED'::text, 'REJECTED'::text, 'TIMED_OUT'::text])))
- ddl | schema/baseline.sql | +: CONSTRAINT supervisor_approval_queue_status_check CHECK ((status = ANY (ARRAY['PENDING_SUPERVISOR_APPROVAL'::text, 'APPROVED'::text, 'REJECTED'::text, 'TIMED_OUT'::text, 'ESCALATED'::text, 'RESET'::text])))
- ddl | schema/baseline.sql | +: CREATE TABLE public.supervisor_interrupt_audit_events (
- ddl | schema/baseline.sql | +: CONSTRAINT supervisor_interrupt_audit_events_action_check CHECK ((action = ANY (ARRAY['ESCALATED'::text, 'ACKNOWLEDGED'::text, 'RESUMED'::text, 'RESET'::text]))),
- ddl | schema/baseline.sql | +: CONSTRAINT supervisor_interrupt_audit_events_queue_status_check CHECK ((queue_status = ANY (ARRAY['PENDING_SUPERVISOR_APPROVAL'::text, 'APPROVED'::text, 'REJECTED'::text, 'TIMED_OUT'::text, 'ESCALATED'::text, 'RESET'::text])))
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.supervisor_interrupt_audit_events
- ddl | schema/baseline.sql | +: ADD CONSTRAINT supervisor_interrupt_audit_events_pkey PRIMARY KEY (event_id);
- ddl | schema/baseline.sql | +: CREATE INDEX idx_supervisor_interrupt_audit_events_instruction_recorded ON public.supervisor_interrupt_audit_events USING btree (instruction_id, recorded_at DESC);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.supervisor_interrupt_audit_events
- ddl | schema/baseline.sql | +: ADD CONSTRAINT supervisor_interrupt_audit_events_program_id_fkey FOREIGN KEY (program_id) REFERENCES public.programs(program_id) ON DELETE RESTRICT;
- security | schema/baselines/current/0001_baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baselines/current/0001_baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baselines/current/0001_baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baselines/current/0001_baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baselines/current/0001_baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baselines/current/0001_baseline.sql | +: SET search_path TO 'pg_catalog', 'public'

## Mitigation

DDL remains forward-only, the baseline refresh is included in the same branch, and Wave B verifier coverage plus local preflight are used to constrain the change.
