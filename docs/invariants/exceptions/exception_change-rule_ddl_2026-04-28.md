---
exception_id: EXC-20260428-EXPAND-CONTRACT-REM01
inv_scope: change-rule
expiry: 2026-10-28
follow_up_ticket: REM-2026-04-27_pre_ci-phase1_db_verifiers
reason: Replaced destructive DROP TABLE with idempotent DO blocks in 0165 and 0166 to comply with INV-097 expand/contract policy. The table CREATE statements remain unchanged; only the idempotency mechanism changed from DROP TABLE to IF NOT EXISTS check.
author: db_foundation
created_at: 2026-04-28
---

# Exception: ddl structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

Migrations 0165 and 0166 were modified to replace `DROP TABLE IF EXISTS CASCADE` with idempotent `DO` blocks using `IF NOT EXISTS` checks. This change was required to comply with INV-097 (I-MIG-PAC-01) expand/contract policy, which forbids destructive DDL in Phase-0. The table CREATE statements and all constraints remain unchanged; only the idempotency mechanism was updated to be non-destructive.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: ddl
reason_types: ddl

Matched files:
- schema/migrations/0165_create_public_keys_registry.sql
- schema/migrations/0166_create_delegated_signing_grants.sql

Top matches:
- ddl | schema/migrations/0165_create_public_keys_registry.sql | -: DROP TABLE IF EXISTS public_keys_registry CASCADE;
- ddl | schema/migrations/0165_create_public_keys_registry.sql | -: CREATE TABLE public_keys_registry (
- ddl | schema/migrations/0165_create_public_keys_registry.sql | -: key_type VARCHAR(50) NOT NULL CHECK (key_type IN ('RSA', 'ECDSA', 'ED25519')),
- ddl | schema/migrations/0165_create_public_keys_registry.sql | -: CONSTRAINT valid_until_after_valid_from CHECK (valid_until > valid_from)
- ddl | schema/migrations/0165_create_public_keys_registry.sql | +: CREATE TABLE public_keys_registry (
- ddl | schema/migrations/0165_create_public_keys_registry.sql | +: key_type VARCHAR(50) NOT NULL CHECK (key_type IN ('RSA', 'ECDSA', 'ED25519')),
- ddl | schema/migrations/0165_create_public_keys_registry.sql | +: CONSTRAINT valid_until_after_valid_from CHECK (valid_until > valid_from)
- ddl | schema/migrations/0166_create_delegated_signing_grants.sql | -: DROP TABLE IF EXISTS delegated_signing_grants CASCADE;
- ddl | schema/migrations/0166_create_delegated_signing_grants.sql | -: CREATE TABLE delegated_signing_grants (
- ddl | schema/migrations/0166_create_delegated_signing_grants.sql | -: CONSTRAINT expires_after_granted CHECK (expires_at > granted_at),
- ddl | schema/migrations/0166_create_delegated_signing_grants.sql | -: CONSTRAINT payload_hash_format CHECK (payload_hash ~ '^[a-f0-9]{64}$')
- ddl | schema/migrations/0166_create_delegated_signing_grants.sql | +: CREATE TABLE delegated_signing_grants (
- ddl | schema/migrations/0166_create_delegated_signing_grants.sql | +: CONSTRAINT expires_after_granted CHECK (expires_at > granted_at),
- ddl | schema/migrations/0166_create_delegated_signing_grants.sql | +: CONSTRAINT payload_hash_format CHECK (payload_hash ~ '^[a-f0-9]{64}$')

## Mitigation

[Describe any mitigating controls in place]
