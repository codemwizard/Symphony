---
exception_id: EXC-902
inv_scope: change-rule
expiry: 2026-12-31
follow_up_ticket: TSK-P0-LEVY-003
reason: Auto-generated: structural change detected; invariants linkage missing in commit.
author: system
created_at: 2026-02-22
---

# Exception: ddl structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

TSK-P0-LEVY-003 introduces a forward-only schema migration and baseline refresh for a
new Phase-0 structural hook (`levy_calculation_records`). The commit touches DDL
and baseline files, so the structural change detector requires explicit exception
metadata.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: ddl
reason_types: ddl, migration_file_added_or_deleted

Matched files:
- schema/migrations/0037_levy_calculation_records_hook.sql

Top matches:
- ddl | schema/baseline.sql | +: CREATE TABLE public.levy_calculation_records (
- ddl | schema/baseline.sql | +: CONSTRAINT levy_calculation_records_cap_applied_minor_check CHECK (((cap_applied_minor IS NULL) OR (cap_applied_minor >= 0))),
- ddl | schema/baseline.sql | +: CONSTRAINT levy_calculation_records_levy_amount_final_check CHECK (((levy_amount_final IS NULL) OR (levy_amount_final >= 0))),
- ddl | schema/baseline.sql | +: CONSTRAINT levy_calculation_records_levy_amount_pre_cap_check CHECK (((levy_amount_pre_cap IS NULL) OR (levy_amount_pre_cap >= 0))),
- ddl | schema/baseline.sql | +: CONSTRAINT levy_calculation_records_reporting_period_check CHECK (((reporting_period IS NULL) OR (reporting_period ~ '^\\d{4}-\\d{2}$'::text))),
- ddl | schema/baseline.sql | +: CONSTRAINT levy_calculation_records_taxable_amount_minor_check CHECK (((taxable_amount_minor IS NULL) OR (taxable_amount_minor >= 0)))
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.levy_calculation_records
- ddl | schema/baseline.sql | +: ADD CONSTRAINT levy_calculation_one_per_instruction UNIQUE (instruction_id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.levy_calculation_records
- ddl | schema/baseline.sql | +: ADD CONSTRAINT levy_calculation_records_pkey PRIMARY KEY (id);
- ddl | schema/baseline.sql | +: CREATE INDEX levy_calc_reporting_period_idx ON public.levy_calculation_records USING btree (reporting_period, jurisdiction_code) WHERE (reporting_period IS NOT NULL);
- ddl | schema/baseline.sql | +: CREATE INDEX levy_calc_status_idx ON public.levy_calculation_records USING btree (levy_status) WHERE (levy_status IS NOT NULL);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.levy_calculation_records
- ddl | schema/baseline.sql | +: ADD CONSTRAINT levy_calculation_records_instruction_id_fkey FOREIGN KEY (instruction_id) REFERENCES public.ingress_attestations(attestation_id) ON DELETE RESTRICT;
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.levy_calculation_records
- ddl | schema/baseline.sql | +: ADD CONSTRAINT levy_calculation_records_levy_rate_id_fkey FOREIGN KEY (levy_rate_id) REFERENCES public.levy_rates(id) ON DELETE RESTRICT;
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: CREATE TABLE public.levy_calculation_records (
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: CONSTRAINT levy_calculation_records_cap_applied_minor_check CHECK (((cap_applied_minor IS NULL) OR (cap_applied_minor >= 0))),
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: CONSTRAINT levy_calculation_records_levy_amount_final_check CHECK (((levy_amount_final IS NULL) OR (levy_amount_final >= 0))),
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: CONSTRAINT levy_calculation_records_levy_amount_pre_cap_check CHECK (((levy_amount_pre_cap IS NULL) OR (levy_amount_pre_cap >= 0))),
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: CONSTRAINT levy_calculation_records_reporting_period_check CHECK (((reporting_period IS NULL) OR (reporting_period ~ '^\\d{4}-\\d{2}$'::text))),
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: CONSTRAINT levy_calculation_records_taxable_amount_minor_check CHECK (((taxable_amount_minor IS NULL) OR (taxable_amount_minor >= 0)))
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: ALTER TABLE ONLY public.levy_calculation_records
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: ADD CONSTRAINT levy_calculation_one_per_instruction UNIQUE (instruction_id);
- ddl | schema/baselines/2026-02-22/0001_baseline.sql | +: ALTER TABLE ONLY public.levy_calculation_records

## Mitigation

- `RUN_PHASE1_GATES=1 scripts/dev/pre_ci.sh` passes locally after adding migration,
  verifier, contract row, and baseline refresh.
- The new verifier `scripts/db/verify_levy_calculation_records_hook.sh` is wired in
  `scripts/dev/pre_ci.sh` and fail-closed on schema/runtime drift.
