---
exception_id: EXC-20260309-CQRS-BASELINE
inv_scope: change-rule
expiry: 2026-12-31
follow_up_ticket: PROJ-001
reason: Sprint 2 CQRS/projection baseline refresh adds canonical baseline snapshot artifacts after 0070; regulated doc linkage and invariant mapping are already present in the wave commit.
author: codex
created_at: 2026-03-09
---

# Exception: migration_file_added_or_deleted structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

The amend only stages the regenerated baseline snapshot artifacts (`schema/baseline.sql`, `schema/baselines/current/**`, `schema/baselines/2026-03-09/**`) after the Sprint 2 wave commit already added the CQRS/projection invariant links and architecture/compliance updates. The staged-only preflight therefore sees structural baseline files without the already-committed linkage docs and emits this exception.

## Evidence

structural_change: True
confidence_hint: 0.7
primary_reason: migration_file_added_or_deleted
reason_types: migration_file_added_or_deleted

Matched files:
- schema/baselines/2026-03-09/baseline.normalized.sql

Top matches:
- ddl | schema/baseline.sql | +: CREATE TABLE public.escrow_summary_projection (
- ddl | schema/baseline.sql | +: CREATE TABLE public.evidence_bundle_projection (
- ddl | schema/baseline.sql | +: CREATE TABLE public.incident_case_projection (
- ddl | schema/baseline.sql | +: CREATE TABLE public.instruction_status_projection (
- ddl | schema/baseline.sql | +: CREATE TABLE public.program_member_summary_projection (
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.escrow_summary_projection
- ddl | schema/baseline.sql | +: ADD CONSTRAINT escrow_summary_projection_pkey PRIMARY KEY (escrow_id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.evidence_bundle_projection
- ddl | schema/baseline.sql | +: ADD CONSTRAINT evidence_bundle_projection_pkey PRIMARY KEY (instruction_id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.incident_case_projection
- ddl | schema/baseline.sql | +: ADD CONSTRAINT incident_case_projection_pkey PRIMARY KEY (incident_id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.instruction_status_projection
- ddl | schema/baseline.sql | +: ADD CONSTRAINT instruction_status_projection_pkey PRIMARY KEY (instruction_id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.program_member_summary_projection
- ddl | schema/baseline.sql | +: ADD CONSTRAINT program_member_summary_projection_pkey PRIMARY KEY (tenant_id, program_id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.escrow_summary_projection
- ddl | schema/baseline.sql | +: ADD CONSTRAINT escrow_summary_projection_escrow_id_fkey FOREIGN KEY (escrow_id) REFERENCES public.escrow_accounts(escrow_id) ON DELETE RESTRICT;
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.escrow_summary_projection
- ddl | schema/baseline.sql | +: ADD CONSTRAINT escrow_summary_projection_program_id_fkey FOREIGN KEY (program_id) REFERENCES public.programs(program_id) ON DELETE RESTRICT;
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.escrow_summary_projection
- ddl | schema/baseline.sql | +: ADD CONSTRAINT escrow_summary_projection_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(tenant_id) ON DELETE RESTRICT;
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.evidence_bundle_projection
- ddl | schema/baseline.sql | +: ADD CONSTRAINT evidence_bundle_projection_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(tenant_id) ON DELETE RESTRICT;
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.incident_case_projection
- ddl | schema/baseline.sql | +: ADD CONSTRAINT incident_case_projection_incident_id_fkey FOREIGN KEY (incident_id) REFERENCES public.regulatory_incidents(incident_id) ON DELETE RESTRICT;

## Mitigation

- `docs/architecture/THREAT_MODEL.md` and `docs/architecture/COMPLIANCE_MAP.md` were updated in the Sprint 2 wave commit to document the CQRS/projection posture.
- `docs/invariants/INVARIANTS_MANIFEST.yml`, `docs/invariants/INVARIANTS_IMPLEMENTED.md`, `docs/invariants/INVARIANTS_QUICK.md`, and `docs/PHASE1/phase1_contract.yml` already bind `INV-148`..`INV-151` to the new verifiers and evidence paths.
- `scripts/dev/pre_ci.sh` still enforces structural doc linkage, evidence schema validation, and baseline drift checks on the full branch diff before push.
