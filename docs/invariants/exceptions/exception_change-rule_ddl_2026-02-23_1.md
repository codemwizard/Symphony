---
exception_id: EXC-207
inv_scope: change-rule
expiry: 2026-03-09
follow_up_ticket: TSK-P0-KYC-001
reason: Forward-only DDL hardening migration for KYC provider registry uniqueness semantics and verifier fail-closed behavior.
author: codex
created_at: 2026-02-23
closed_at: 2026-03-09
---

# Exception: ddl structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

This change introduces a new forward-only migration and baseline refresh to remove conflicting full
uniqueness on `(jurisdiction_code, provider_code)` while preserving the active-row partial unique
index used by the Phase-0/Phase-2 model.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: ddl
reason_types: ddl, migration_file_added_or_deleted

Matched files:
- schema/migrations/0041_kyc_provider_registry_drop_conflicting_uniqueness.sql

Top matches:
- ddl | schema/baseline.sql | -: ALTER TABLE ONLY public.kyc_provider_registry
- ddl | schema/baseline.sql | -: ADD CONSTRAINT kyc_provider_unique_active_per_jurisdiction UNIQUE (jurisdiction_code, provider_code);
- ddl | schema/baselines/2026-02-23/0001_baseline.sql | -: ALTER TABLE ONLY public.kyc_provider_registry
- ddl | schema/baselines/2026-02-23/0001_baseline.sql | -: ADD CONSTRAINT kyc_provider_unique_active_per_jurisdiction UNIQUE (jurisdiction_code, provider_code);
- ddl | schema/baselines/2026-02-23/baseline.normalized.sql | -: ADD CONSTRAINT kyc_provider_unique_active_per_jurisdiction UNIQUE (jurisdiction_code, provider_code);
- ddl | schema/baselines/2026-02-23/baseline.normalized.sql | -: ALTER TABLE ONLY public.kyc_provider_registry
- ddl | schema/baselines/current/0001_baseline.sql | -: ALTER TABLE ONLY public.kyc_provider_registry
- ddl | schema/baselines/current/0001_baseline.sql | -: ADD CONSTRAINT kyc_provider_unique_active_per_jurisdiction UNIQUE (jurisdiction_code, provider_code);
- ddl | schema/migrations/0041_kyc_provider_registry_drop_conflicting_uniqueness.sql | +: ALTER TABLE public.kyc_provider_registry
- ddl | schema/migrations/0041_kyc_provider_registry_drop_conflicting_uniqueness.sql | +: DROP CONSTRAINT IF EXISTS kyc_provider_unique_active_per_jurisdiction;
- migration_file_added_or_deleted | schema/migrations/0041_kyc_provider_registry_drop_conflicting_uniqueness.sql | +: (migration file added or deleted)

## Mitigation

- `scripts/db/verify_kyc_provider_registry_hook.sh` now fails if the conflicting full uniqueness
  constraint still exists.
- Migration checksum validation is fail-closed and now gated by migration-framework presence.
- Full `RUN_PHASE1_GATES=1 scripts/dev/pre_ci.sh` passed locally after baseline regeneration.
