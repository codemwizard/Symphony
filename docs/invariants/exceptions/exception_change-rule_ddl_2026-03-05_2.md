---
exception_id: EXC-312
inv_scope: change-rule
expiry: 2026-12-31
closed_at: 2026-03-21
follow_up_ticket: TSK-HARD-020-026
reason: Auto-generated: structural change detected; invariants linkage missing in commit.
author: system
created_at: 2026-03-05
---

# Exception: ddl structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

Wave-2 introduces required new adjustment-governance DDL and baseline refresh in one batch.
This temporary exception tracks the structural delta until invariants linkage is promoted in follow-on reconciliation.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: ddl
reason_types: ddl, migration_file_added_or_deleted, security

Matched files:
- schema/migrations/0063_hard_wave2_adjustment_governance.sql

Top matches:
- ddl | schema/baseline.sql | +: CREATE TYPE public.adjustment_state_enum AS ENUM (
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- ddl | schema/baseline.sql | +: CREATE TABLE public.adjustment_approval_stages (
- ddl | schema/baseline.sql | +: CREATE TABLE public.adjustment_approvals (
- ddl | schema/baseline.sql | +: CREATE TABLE public.adjustment_execution_attempts (
- ddl | schema/baseline.sql | +: CREATE TABLE public.adjustment_freeze_flags (
- ddl | schema/baseline.sql | +: CREATE TABLE public.adjustment_instructions (
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.adjustment_approval_stages
- ddl | schema/baseline.sql | +: ADD CONSTRAINT adjustment_approval_stages_pkey PRIMARY KEY (stage_id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.adjustment_approvals
- ddl | schema/baseline.sql | +: ADD CONSTRAINT adjustment_approvals_pkey PRIMARY KEY (approval_id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.adjustment_approvals
- ddl | schema/baseline.sql | +: ADD CONSTRAINT adjustment_approvals_stage_id_approver_id_key UNIQUE (stage_id, approver_id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.adjustment_execution_attempts
- ddl | schema/baseline.sql | +: ADD CONSTRAINT adjustment_execution_attempts_adjustment_id_idempotency_key_key UNIQUE (adjustment_id, idempotency_key);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.adjustment_execution_attempts

## Mitigation

Mitigations in this batch:
- Full local `RUN_PHASE1_GATES=1 scripts/dev/pre_ci.sh` pass
- Task-specific Wave-2 verifiers and evidence artifacts
- SQLSTATE map + ADR baseline updates committed with the migration
