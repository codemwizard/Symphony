---
exception_id: EXC-903
inv_scope: change-rule
expiry: 2026-12-31
follow_up_ticket: TSK-P1-HIER-003
reason: Auto-generated: structural change detected; invariants linkage missing in commit.
author: system
created_at: 2026-02-24
closed_at: 2026-03-10
---

# Exception: ddl structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

Structural change includes a new Phase-1 migration (`0048`) plus synchronized baseline snapshots.
The change is task-scoped and includes verifier evidence and governance doc updates; invariant-manifest
expansion for hierarchy-series IDs is tracked as follow-up under the same task chain.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: ddl
reason_types: ddl, migration_file_added_or_deleted, security

Matched files:
- schema/migrations/0048_hier_003_member_devices_distribution_ceiling.sql

Top matches:
- ddl | schema/baseline.sql | +: CREATE TABLE public.member_devices (
- ddl | schema/baseline.sql | +: CONSTRAINT member_devices_status_check CHECK ((status = ANY (ARRAY['ACTIVE'::text, 'INACTIVE'::text, 'REVOKED'::text])))
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.member_devices
- ddl | schema/baseline.sql | +: ADD CONSTRAINT member_devices_pkey PRIMARY KEY (member_id, device_id_hash);
- ddl | schema/baseline.sql | +: CREATE INDEX idx_member_devices_active_device ON public.member_devices USING btree (tenant_id, device_id_hash) WHERE (status = 'ACTIVE'::text);
- ddl | schema/baseline.sql | +: CREATE INDEX idx_member_devices_active_iccid ON public.member_devices USING btree (tenant_id, iccid_hash) WHERE ((iccid_hash IS NOT NULL) AND (status = 'ACTIVE'::text));
- ddl | schema/baseline.sql | +: CREATE INDEX idx_member_devices_tenant_member ON public.member_devices USING btree (tenant_id, member_id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.member_devices
- ddl | schema/baseline.sql | +: ADD CONSTRAINT member_devices_member_id_fkey FOREIGN KEY (member_id) REFERENCES public.members(member_id) ON DELETE RESTRICT;
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.member_devices
- ddl | schema/baseline.sql | +: ADD CONSTRAINT member_devices_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(tenant_id) ON DELETE RESTRICT;
- ddl | schema/baselines/2026-02-24/0001_baseline.sql | +: CREATE TABLE public.member_devices (
- ddl | schema/baselines/2026-02-24/0001_baseline.sql | +: CONSTRAINT member_devices_status_check CHECK ((status = ANY (ARRAY['ACTIVE'::text, 'INACTIVE'::text, 'REVOKED'::text])))
- ddl | schema/baselines/2026-02-24/0001_baseline.sql | +: ALTER TABLE ONLY public.member_devices
- ddl | schema/baselines/2026-02-24/0001_baseline.sql | +: ADD CONSTRAINT member_devices_pkey PRIMARY KEY (member_id, device_id_hash);
- ddl | schema/baselines/2026-02-24/0001_baseline.sql | +: CREATE INDEX idx_member_devices_active_device ON public.member_devices USING btree (tenant_id, device_id_hash) WHERE (status = 'ACTIVE'::text);
- ddl | schema/baselines/2026-02-24/0001_baseline.sql | +: CREATE INDEX idx_member_devices_active_iccid ON public.member_devices USING btree (tenant_id, iccid_hash) WHERE ((iccid_hash IS NOT NULL) AND (status = 'ACTIVE'::text));
- ddl | schema/baselines/2026-02-24/0001_baseline.sql | +: CREATE INDEX idx_member_devices_tenant_member ON public.member_devices USING btree (tenant_id, member_id);
- ddl | schema/baselines/2026-02-24/0001_baseline.sql | +: ALTER TABLE ONLY public.member_devices
- ddl | schema/baselines/2026-02-24/0001_baseline.sql | +: ADD CONSTRAINT member_devices_member_id_fkey FOREIGN KEY (member_id) REFERENCES public.members(member_id) ON DELETE RESTRICT;
- ddl | schema/baselines/2026-02-24/0001_baseline.sql | +: ALTER TABLE ONLY public.member_devices
- ddl | schema/baselines/2026-02-24/0001_baseline.sql | +: ADD CONSTRAINT member_devices_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(tenant_id) ON DELETE RESTRICT;
- ddl | schema/baselines/2026-02-24/baseline.normalized.sql | +: ADD CONSTRAINT member_devices_member_id_fkey FOREIGN KEY (member_id) REFERENCES public.members(member_id) ON DELETE RESTRICT;
- ddl | schema/baselines/2026-02-24/baseline.normalized.sql | +: ADD CONSTRAINT member_devices_pkey PRIMARY KEY (member_id, device_id_hash);
- ddl | schema/baselines/2026-02-24/baseline.normalized.sql | +: ADD CONSTRAINT member_devices_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(tenant_id) ON DELETE RESTRICT;

## Mitigation

- Added explicit compliance/threat model linkage notes for the new member-device structural surface.
- Regenerated baseline snapshot and updated ADR-0010 baseline governance log.
- Added task verifier `scripts/db/verify_tsk_p1_hier_003.sh` and produced deterministic PASS evidence.
