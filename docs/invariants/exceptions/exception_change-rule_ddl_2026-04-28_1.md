---
exception_id: EXC-028
inv_scope: change-rule
expiry: 2026-05-12
follow_up_ticket: N/A
reason: Comment-only change to restore PK/FK waiver marker; no actual DDL change.
author: system
created_at: 2026-04-28
---

# Exception: ddl structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

The change to 0171_attestation_kill_switch_gate.sql is a comment-only change to restore the PK/FK type change waiver marker that was removed during merge conflict resolution. No actual DDL was modified - only the waiver comment was restored to satisfy lint_pk_fk_type_changes.sh. The structural change detector flagged the file due to the ALTER TABLE statement present in the file, but the actual SQL content was not changed.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: ddl
reason_types: ddl

Matched files:
- schema/migrations/0171_attestation_kill_switch_gate.sql

Top matches:
- ddl | schema/migrations/0171_attestation_kill_switch_gate.sql | -: ALTER TABLE public.asset_batches
- ddl | schema/migrations/0171_attestation_kill_switch_gate.sql | +: ALTER TABLE public.asset_batches

## Mitigation

The change is comment-only (waiver marker restoration). The actual DDL statement (ALTER TABLE ... ALTER COLUMN ... TYPE) was already present in the file before this change. The waiver marker is required to pass lint_pk_fk_type_changes.sh since invariant_attestation_hash is not a PK/FK column. No functional change to the database schema.
