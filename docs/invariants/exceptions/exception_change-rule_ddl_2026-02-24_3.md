---
exception_id: EXC-2026-02-24-HIER-007-01
inv_scope: change-rule
expiry: 2026-12-31
follow_up_ticket: TSK-P1-HIER-007
reason: Auto-generated: structural change detected; invariants linkage missing in commit.
author: system
created_at: 2026-02-24
closed_at: 2026-03-10
---

# Exception: ddl structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

TSK-P1-HIER-007 requires new structural DDL for risk formula registry and program migration events.
The implementation includes verifier-backed evidence and full gate execution, but Rule-1 requires
explicit exception metadata for this structural change in commit scope.

## Evidence

structural_change: True
confidence_hint: 1.0
primary_reason: ddl
reason_types: ddl, migration_file_added_or_deleted, security

Matched files:
- schema/migrations/0052_hier_007_risk_formula_registry_program_migration.sql
- scripts/db/verify_tsk_p1_hier_007.sh

Top matches:
- security | docs/plans/phase1/TSK-P1-HIER-007/PLAN.md | +: - `migrate_person_to_program(...)` (SECURITY DEFINER + hardened search_path)
- security | evidence/phase1/tsk_p1_hier_007__risk_formula_registry_tier_deterministic_default.json | +: "detail": "migrate_person_to_program is SECURITY DEFINER with hardened search_path"
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- ddl | schema/baseline.sql | +: CREATE TABLE public.program_migration_events (
- ddl | schema/baseline.sql | +: CONSTRAINT program_migration_events_from_to_chk CHECK ((from_program_id <> to_program_id))
- ddl | schema/baseline.sql | +: CREATE TABLE public.risk_formula_versions (
- ddl | schema/baseline.sql | +: CONSTRAINT risk_formula_versions_tier_check CHECK ((tier = ANY (ARRAY['TIER1'::text, 'TIER2'::text, 'TIER3'::text])))
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.program_migration_events
- ddl | schema/baseline.sql | +: ADD CONSTRAINT program_migration_events_pkey PRIMARY KEY (migration_event_id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.risk_formula_versions
- ddl | schema/baseline.sql | +: ADD CONSTRAINT risk_formula_versions_formula_key_key UNIQUE (formula_key);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.risk_formula_versions
- ddl | schema/baseline.sql | +: ADD CONSTRAINT risk_formula_versions_pkey PRIMARY KEY (formula_version_id);
- ddl | schema/baseline.sql | +: CREATE INDEX idx_program_migration_events_tenant_person ON public.program_migration_events USING btree (tenant_id, person_id, migrated_at DESC);
- ddl | schema/baseline.sql | +: CREATE INDEX idx_program_migration_events_tenant_time ON public.program_migration_events USING btree (tenant_id, migrated_at DESC);
- ddl | schema/baseline.sql | +: CREATE UNIQUE INDEX ux_program_migration_events_deterministic ON public.program_migration_events USING btree (tenant_id, person_id, from_program_id, to_program_id);
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.program_migration_events
- ddl | schema/baseline.sql | +: ADD CONSTRAINT program_migration_events_formula_version_id_fkey FOREIGN KEY (formula_version_id) REFERENCES public.risk_formula_versions(formula_version_id) ON DELETE RESTRICT;
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.program_migration_events
- ddl | schema/baseline.sql | +: ADD CONSTRAINT program_migration_events_from_program_id_fkey FOREIGN KEY (from_program_id) REFERENCES public.programs(program_id) ON DELETE RESTRICT;
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.program_migration_events
- ddl | schema/baseline.sql | +: ADD CONSTRAINT program_migration_events_migrated_member_id_fkey FOREIGN KEY (migrated_member_id) REFERENCES public.members(member_id) ON DELETE RESTRICT;
- ddl | schema/baseline.sql | +: ALTER TABLE ONLY public.program_migration_events
- ddl | schema/baseline.sql | +: ADD CONSTRAINT program_migration_events_person_id_fkey FOREIGN KEY (person_id) REFERENCES public.persons(person_id) ON DELETE RESTRICT;

## Mitigation

- Forward-only migration `0052_hier_007_risk_formula_registry_program_migration.sql`.
- SECURITY DEFINER function hardened with `SET search_path = pg_catalog, public`.
- Revoke-first posture applied on new tables/views.
- Task verifier `scripts/db/verify_tsk_p1_hier_007.sh` and `RUN_PHASE1_GATES=1 scripts/dev/pre_ci.sh` passed.
