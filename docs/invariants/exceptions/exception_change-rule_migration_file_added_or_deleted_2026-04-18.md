---
exception_id: EXC-000
inv_scope: change-rule
expiry: 2026-05-02
follow_up_ticket: PLACEHOLDER-000
reason: Auto-generated: structural change detected; invariants linkage missing in commit.
author: system
created_at: 2026-04-18
---

# Exception: migration_file_added_or_deleted structural change without invariants linkage

This exception was auto-generated by local preflight because a structural change was detected,
but invariants linkage (manifest/docs with INV-###) was not included in the same commit.

## Reason

This exception covers the baseline refresh for Wave 3 Phase 2 implementation. The dated baseline snapshot at schema/baselines/2026-04-18/ was added to include migration 0118 (execution_records table with interpretation_version_id FK). The baseline governance policy requires baseline refresh when migrations are added to maintain consistency between baseline and current schema. This is a baseline refresh operation, not a new independent schema change - the schema content is derived from the existing migrations 0001-0118.

## Evidence

structural_change: True
confidence_hint: 0.7
primary_reason: migration_file_added_or_deleted
reason_types: migration_file_added_or_deleted

Matched files:
- schema/baselines/2026-04-18/baseline.normalized.sql

Top matches:
- security | schema/baseline.sql | +: SELECT pg_catalog.set_config('search_path', '', false);
- ddl | schema/baseline.sql | +: CREATE TYPE public.adjustment_state_enum AS ENUM (
- ddl | schema/baseline.sql | +: CREATE TYPE public.finality_resolution_state_enum AS ENUM (
- ddl | schema/baseline.sql | +: CREATE TYPE public.finality_signal_status_enum AS ENUM (
- ddl | schema/baseline.sql | +: CREATE TYPE public.inquiry_state_enum AS ENUM (
- ddl | schema/baseline.sql | +: CREATE TYPE public.key_class_enum AS ENUM (
- ddl | schema/baseline.sql | +: CREATE TYPE public.orphan_classification_enum AS ENUM (
- ddl | schema/baseline.sql | +: CREATE TYPE public.outbox_attempt_state AS ENUM (
- ddl | schema/baseline.sql | +: CREATE TYPE public.policy_bundle_state_enum AS ENUM (
- ddl | schema/baseline.sql | +: CREATE TYPE public.policy_version_status AS ENUM (
- ddl | schema/baseline.sql | +: CREATE TYPE public.quarantine_classification_enum AS ENUM (
- ddl | schema/baseline.sql | +: CREATE TYPE public.reference_strategy_type_enum AS ENUM (
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER
- security | schema/baseline.sql | +: SET search_path TO 'pg_catalog', 'public'
- security | schema/baseline.sql | +: LANGUAGE plpgsql SECURITY DEFINER

## Mitigation

- Baseline was regenerated using generate_baseline_snapshot.sh from a database with all migrations 0001-0118 applied
- Baseline cutoff updated to 0118_create_execution_records.sql to match the latest migration
- Baseline drift check passes after refresh
- Approval metadata already exists for Wave 3 regulated surface changes (evidence/phase1/approval_metadata.json)
- Threat model and compliance map already updated for execution_records table in previous commits
