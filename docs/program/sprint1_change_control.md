# Sprint 1 Change Control

Every Sprint 1 task implementation must declare:
- blast radius
- protected/no-touch surfaces affected
- verifier coverage added or reused
- evidence artifact path
- rollback posture

Sprint 1 is complete only when the canonical task set is implemented with verifier-backed evidence and passes `scripts/dev/pre_ci.sh`.
