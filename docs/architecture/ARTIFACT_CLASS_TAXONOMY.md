# Artifact Class Taxonomy

- `INDIVIDUAL_SIGNED`: Single artifact envelope signed independently.
- `BATCH_MERKLE_SIGNED`: Batch artifact set anchored by Merkle root and signed once.

Both classes are validated under fail-closed verification and evidence retention requirements.
