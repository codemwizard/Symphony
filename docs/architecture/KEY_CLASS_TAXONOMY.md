# Key Class Taxonomy

Canonical-Reference: docs/operations/AI_AGENT_OPERATION_MANUAL.md

Classes: `EASK`, `PCSK`, `AAK`, `TRANSPORT_IDENTITY`.
Authorization is enforced by `public.signing_authorization_matrix` and runtime
checks in `public.assert_key_class_authorized`.
