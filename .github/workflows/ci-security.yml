

name: Symphony Security & CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  analyze:
    name: CodeQL Analysis
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Enforce Phase Monotonicity
      shell: bash
      run: |
        git fetch origin main
    
        CURRENT_PHASE=$(git show origin/main:.symphony/PHASE 2>/dev/null || echo "6")
        PROPOSED_PHASE=$(cat .symphony/PHASE 2>/dev/null || echo "6")
    
        echo "Current Phase (main): $CURRENT_PHASE"
        echo "Proposed Phase (branch): $PROPOSED_PHASE"
    
        if [ "$CURRENT_PHASE" -ge 7 ] && [ "$PROPOSED_PHASE" -lt "$CURRENT_PHASE" ]; then
          echo "::error::Phase downgrade detected. Phase is monotonic once Phase >=7."
          exit 1
        fi

    - name: Enforce Phase Monotonicity
      shell: bash
      run: |
        git fetch origin main
    
        CURRENT_PHASE=$(git show origin/main:.symphony/PHASE 2>/dev/null || echo "6")
        PROPOSED_PHASE=$(cat .symphony/PHASE 2>/dev/null || echo "6")
    
        echo "Current Phase (main): $CURRENT_PHASE"
        echo "Proposed Phase (branch): $PROPOSED_PHASE"
    
        if [ "$CURRENT_PHASE" -ge 7 ] && [ "$PROPOSED_PHASE" -lt "$CURRENT_PHASE" ]; then
          echo "::error::Phase downgrade detected. Phase is monotonic once >=7."
          exit 1
        fi

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v4
      with:
        languages: javascript-typescript

    - name: Autobuild
      uses: github/codeql-action/autobuild@v4

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v4

  security-gates:
    name: Security Invariants & Tests
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: 'recursive'
        fetch-depth: 0  # Full history for drift detection

    - name: Ensure Policy Lock Exists
      run: |
        test -f .policy.lock || (
          echo "❌ .policy.lock missing — policy version must be explicitly pinned."
          exit 1
        )

    - name: Block Unapproved Policy Changes
      run: |
        git fetch origin main --quiet
        git diff --name-only origin/main...HEAD | grep "^\.policies/" && (
          echo "❌ Direct policy modification detected."
          echo "Policy updates must be approved upstream in org-security-policies."
          exit 1
        ) || true

    - name: Verify Policy Version Lock
      run: |
        LOCKED=$(grep "commit:" .policy.lock | awk '{print $2}')
        ACTUAL=$(cd .policies && git rev-parse HEAD)
        if [ "$LOCKED" != "$ACTUAL" ]; then
          echo "❌ Policy version mismatch"
          echo "Locked: $LOCKED"
          echo "Actual: $ACTUAL"
          echo "Update .policy.lock via approved process."
          exit 1
        fi
        echo "✅ Policy version verified: $LOCKED"

    - name: Validate Policies Loaded
      run: |
        test -d ".policies" || (echo "❌ Policies not loaded" && exit 1)
        echo "✅ Security policies available at .policies/"




    - name: Detect Phase
      run: |
        if [ -f .symphony/PHASE ]; then
          PHASE=$(cat .symphony/PHASE | tr -d '[:space:]')
        else
          PHASE=6
        fi
        echo "PHASE=$PHASE" >> $GITHUB_ENV
    
    - name: Use Node.js 20
      uses: actions/setup-node@v4
      with:
        node-version: 20
        cache: 'npm'
        
    - name: Install Dependencies
      run: npm ci
      
    - name: Run Security Gates (Invariant Checks)
      run: npm run security-check
      
    - name: Run Unit Tests
      run: npm test

    - name: Run Compliance Verification
      run: npm run ci:compliance

    - name: Install Snyk CLI
      run: npm install -g snyk
    
    - name: Verify Snyk Installation
      run: snyk --version

Final Review:

[CI-Evidence-Bundle] Implementation Plan
Phase Name: CI-Evidence-Bundle Phase Key: EVID-001 Date: 2026-01-12 Status: APPROVED with amendments

Goal
Implement regulator-grade CI Evidence Bundle system that produces deterministic, immutable, cryptographically-traceable evidence artifacts on every build.

Required Amendments (from review)
#	Amendment	Reason
1	Add evidence_bundle_version as required field	Schema evolution without breaking old evidence
2	Pin ajv-cli@5.0.0	Toolchain determinism
3	Add ci_conclusion field to build_attestation	Prevent evidence for failed runs
4	Add enforcement_active + reason to ai_usage	Clarify when AI enforcement is inactive
Proposed Changes
Part 1: CI Governance Fixes
[MODIFY] 
ci-security.yml
Order of checks (corrected):

.policy.lock
 exists ← First
.policies loaded
Hash matches
- name: Ensure Policy Lock Exists
  run: |
    test -f .policy.lock || (
      echo "❌ .policy.lock missing — policy version must be explicitly pinned."
      exit 1
    )
- name: Block Unapproved Policy Changes
  run: |
    git diff --name-only origin/main...HEAD | grep "^\.policies/" && (
      echo "❌ Direct policy modification detected."
      exit 1
    ) || true
- name: Verify Policy Version Lock
  run: |
    LOCKED=$(grep "commit:" .policy.lock | awk '{print $2}')
    ACTUAL=$(cd .policies && git rev-parse HEAD)
    test "$LOCKED" = "$ACTUAL" || exit 1
Part 2: Evidence Bundle Schema
[NEW] schemas/evidence-bundle.schema.json
Key fields (amended):

evidence_bundle_version: "1.0" (const, required)
build_attestation.ci_conclusion (required: "success" | "failure")
ai_usage.enforcement_active (required: boolean)
ai_usage.enforcement_reason (optional: string)
Part 3: Evidence Exporter Scripts
[NEW] .ci/evidence/generate_evidence.sh
Include ci_conclusion: "success" in build_attestation
Include enforcement_active: false + reason when AI enforcement is off
[NEW] .ci/evidence/compute_hash.sh
SHA-256 hash embedded in bundle
[NEW] .ci/evidence/package_bundle.sh
ZIP artifact creation
Part 4: CI Workflow Updates
[MODIFY] ci-security.yml
- name: Generate Evidence Bundle
  run: .ci/evidence/generate_evidence.sh
- name: Compute Evidence Hash
  run: .ci/evidence/compute_hash.sh
- name: Validate Evidence Schema
  run: |
    npx --yes ajv-cli@5.0.0 validate \
      -s schemas/evidence-bundle.schema.json \
      -d evidence-bundle.json \
      --strict=true
- name: Package Evidence Bundle
  run: .ci/evidence/package_bundle.sh
- name: Upload Evidence Artifact
  uses: actions/upload-artifact@v4
  with:
    name: evidence-bundle
    path: |
      evidence-bundle.json
      evidence-bundle.sha256
      evidence-bundle-*.zip
Part 5: AI Declaration (Phase-Gated)
When enforcement is OFF, evidence bundle includes:

"ai_usage": {
  "ai_assisted": "Undeclared",
  "enforcement_active": false,
  "enforcement_reason": "Pre-REGULATED phase"
}
Part 6: CODEOWNERS
[NEW] CODEOWNERS
.policy.lock         @security-team
SECURITY_POLICY.md   @security-team
Verification Plan
Automated
CI runs with all enforcement steps
Evidence bundle generated only after success
Schema validation passes with pinned ajv-cli
Manual
Download evidence-bundle.zip
Verify SHA-256 hash
Confirm ci_conclusion = "success"

CI-Evidence-Bundle Task List
 CI Governance Fixes
 Add policy lock existence check (FIRST, before verification)
 Fix drift detection (origin/main...HEAD)
 Evidence Bundle Schema
 Create schemas/evidence-bundle.schema.json
 Include evidence_bundle_version as required const
 Add ci_conclusion to build_attestation
 Add enforcement_active + enforcement_reason to ai_usage
 Evidence Exporter Scripts
 Create .ci/evidence/generate_evidence.sh
 Create .ci/evidence/compute_hash.sh
 Create .ci/evidence/package_bundle.sh
 CI Workflow Updates
 Pin ajv-cli@5.0.0 for validation
 Add evidence generation steps (after all tests pass)
 CODEOWNERS
 Create CODEOWNERS for 
.policy.lock
 and 
SECURITY_POLICY.md
 Verification
 Run CI and verify evidence bundle generated
 Confirm ci_conclusion = "success" in bundle    - name: Snyk Security Scan (Phase-aware)
      shell: bash
      env:
        SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
      run: |
        if [ "$PHASE" -ge 7 ]; then
          echo "Phase 7: Enforcing Snyk"
          snyk test
        else
          echo "Phase 6: Advisory Snyk"
          snyk test || echo "::warning::Snyk findings or auth issue (Phase 6 advisory)"
        fi

        
    - name: Audit Dependencies
      run: npm audit --audit-level=high || echo "::warning::High severity vulnerabilities found"

    - name: Generate Evidence Bundle
      run: .ci/evidence/generate_evidence.sh

    - name: Compute Evidence Hash
      run: .ci/evidence/compute_hash.sh

    - name: Validate Evidence Schema
      run: |
        npx --yes ajv-cli@5.0.0 validate \
          -s schemas/evidence-bundle.schema.json \
          -d evidence-bundle.json \
          --strict=true

    - name: Package Evidence Bundle
      run: .ci/evidence/package_bundle.sh

    - name: Upload Evidence Bundle
      uses: actions/upload-artifact@v4
      with:
        name: evidence-bundle
        path: |
          evidence-bundle.json
          evidence-bundle.sha256
          evidence-bundle-*.zip

    - name: Upload Phase Evidence
      uses: actions/upload-artifact@v4
      with:
        name: phase-evidence
        path: |
          .symphony/PHASE
          logs/
          test-results/
