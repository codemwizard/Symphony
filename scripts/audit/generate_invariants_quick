#!/usr/bin/env bash
# Wrapper for generate_invariants_quick.py
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/generate_invariants_quick.py" "$@"
