import { describe, it } from 'node:test';
import * as assert from 'node:assert';

console.log('Sanity loading');

describe('Sanity', () => {
    it('should pass', () => {
        assert.ok(true);
    });
});
